## 录屏软件

[Capture录屏](https://www.bilibili.com/video/BV1T64y1c7Wa/?vd_source=7346303e5e18677d7261c2c0c109ecfd) [ShareX](https://getsharex.com/) [shareX](https://zhuanlan.zhihu.com/p/447279675)  [FastStone Capture](https://www.faststonecapture.cn/)  [FastStone Image Viewer](https://baike.baidu.com/item/FastStone%20Image%20Viewer/7178571?fr=ge_ala)  [FastStone 系列](https://blog.csdn.net/u014696856/article/details/134927740)  [录屏软件](https://www.zhihu.com/question/381578560) 

[小鹅通直播助手](https://sem.xiaoe-tech.com/xiaoeLive?jzl_kwd=308433200306&jzl_ctv=65528703359&jzl_ch=11&jzl_act=31888086&jzl_cpg=145871658&jzl_adp=5819625450&jzl_sty=0&jzl_dv=1&bd_vid=5493118988108380923)   [Rancher介绍](https://www.cnblogs.com/zhangxingeng/p/11753959.html)  

## 老男孩3天课程

### 视频

第一天回放：game, https://jjwad.xetlk.com/sl/3YOPFp
第二天回放：可道云,不用mysql； https://jjwad.xetlk.com/sl/3O14n2
第三天回放：LNMP, wordpress,https://jjwad.xetlk.com/sl/1iReLC

### 笔记

```sh
# 2024.5.27  20：00
# todesk 远程控制软件
华为数通证书 红帽RHCE   先考rhce才能考rhca
openstack ceph 网络服务管理  自动化 性能调优

#将从官方下载改到阿里云地址下载：
curl -s -o /etc/yum.repos.d/CentOS-Base.repo \
http://mirrors.aliyun.com/repo/Centos-7.repo

##b.配置第三方的软件下载的epel源更新地址为阿里云的地址：
curl -s -o /etc/yum.repos.d/epel.repo \
http://mirrors.aliyun.com/repo/epel-7.repo

##c.CentOS7要安装的企业运维常用基础工具包
yum install psmisc bash-completion lrzsz wget -y

#1.蒸汽火车游戏
#安装sl命令		yum install sl -y

##安装cowsay命令工具
yum install cowsay -y
老男孩:
##让牛说话游戏
cowsay "今天来听课的同学未来都会非常棒，月薪3万3年之内"
cowsay -f dragon  "老子是一条龙"
cowsay -l


##先跑通，再变通。——老男孩
##1.关闭selinux，它是一个安全模块，不关闭，老管着你。
setenforce 0  ##临时关闭selinux
getenforce    ##查看是否关闭
sed -i 's#SELINUX=enforcing#SELINUX=disabled#g' /etc/selinux/config  ##永久关闭

#2.关闭防火墙，为了学习方便。
systemctl stop firewalld     ##关闭防火墙
systemctl disable firewalld  ##禁止开机自启动
systemctl status firewalld   ##查看关闭结果

老男孩:
#3.安装Nginx Web服务，需要联网
yum install nginx -y
#有 “完毕” 字样表示成功

#4.进入到Linux的nginx web服务下面的游戏站点目录
cd /usr/share/nginx/
html.zip按鼠标拖拽到xshell
#7.解压游戏代码: -o 覆盖												# 安装游戏
unzip -o html*.zip

#8.启动nginx服务并开机自启动
##启动nginx服务
systemctl start nginx
##激活开机自启动
systemctl enable nginx

#9.获取自己Linux服务器的ip地址
ip a 
##看ens33下面的地址，类似
###inet 10.0.0.128/24，要里面的10.0.0.128

rancher # k8s多集群管理
第一天回放：https://jjwad.xetlk.com/sl/3YOPFp
```

第2天： LNP,没用mysql  [可道云KODExplorer](https://doc.kodcloud.com/#/)  [nginx中fastcgi_params配置参数 ](https://www.cnblogs.com/qcwblog/p/8032976.html) 

```sh
# 2024.5.28  20：00
# 百度云盘:linux + nginx +php
# 新设立了 xftp
ss -lnt|grep 80
## 2.把【老男孩老师】提供的php.zip软件包下载到到电脑里
yum install lrzsz -y		# rz拖到linux
unzip -o php*.zip									

## 5.本地安装php软件  #10秒就安装完。
yum localinstall *.rpm -y												# 安装php
##必须有“完毕”字样才算成功。

##6.检查当前PHP服务用户默认配置情况
grep "apache" /etc/php-fpm.d/www.conf

##7.将www.conf里的apache用户修改为nginx用户
sed -i 's#apache#nginx#g' /etc/php-fpm.d/www.conf

##8.检查修改后情况
grep "nginx" /etc/php-fpm.d/www.conf
##成功标志是有3个红色nginx字样

##9.启动php-fpm服务（厨师上岗服务）										# 启动PHP-FPM
systemctl start php-fpm  

##10.加入开机自启
systemctl enable php-fpm

##11.检查是否启动，正常启动了是9000端口
ss -lnt|grep 9000
#成功标志：红色9000

#到此为止PHP服务就配好了。

#12）生成网盘虚拟主机配置，并配置nginx连接php								# 不用mysql
#生成网盘虚拟主机配置，不要用vim编辑，直接复制粘贴。
cat >/etc/nginx/conf.d/pan.etiantian.org.conf<<'EOF' 
server {
        server_name pan.etiantian.org;  ## 临时域名。正式搭建需要购买域名。
        listen 80;                      ## 网页服务端口。
        root /data/pan;                 ## 程序存放目录。
        index index.php index.html;     ## 首页，网页总入口。
        ##符合php扩展名结尾抛给PHP服务处理
        location ~ \.php$ {             ## 以.php结尾的程序发给PHP服务（9000）处理。
            fastcgi_pass   127.0.0.1:9000;  ##发给PHP服务处理。
            fastcgi_index  index.php;
            fastcgi_param  SCRIPT_FILENAME  $document_root$fastcgi_script_name;	#.php文件请求路径
            include        fastcgi_params;		# nginx调用fastcgi_params配置文件，加载fastcgi模块
        }
}
EOF
##检查配置
cat /etc/nginx/conf.d/pan.etiantian.org.conf

#13）检查nginx服务配置的语法，如果没问题就重新启动NGINX
nginx -t                 ## 检查语法，目的确认配置正确。正确标志是syntax is ok
systemctl restart nginx  ## 重启nginx，目的让配置生效。正确标志操作完没有反应。

## 14.创建网盘站点目录，并切到目录下
mkdir /data/pan -p && cd /data/pan
##成功标志是没有任何反应

## 15.从云盘或群里，把kodexplorer4.40.zip程序下载下来，注意不要解压				# 安装kodexplorer4.40

## 16.用鼠标把kodexplorer4.40.zip拖放到linux里的/data/pan下
  ##问题1：如果不成功，可以执行rz上传。
  ##问题2：rz执行提示找不到命令，则安装lrzsz,执行yum install lrzsz -y，然后重复操作。
  ## 17.解压云盘压缩包
unzip -o k*.zip

## 18.授权【nginx用户】访问网盘/data/pan目录
chown -R nginx /data/pan

admin/123456; 普通用户:demo/demo; 游客:guest/guest


##26)解决游戏站点不能访问问题
#（5）解决游戏站点不能访问问题， 再配置一个虚拟主机
cat >/etc/nginx/conf.d/01_game.etiantian.org.conf<<'EOF' 
server {
        server_name game.etiantian.org;
        listen 80;
        root /usr/share/nginx/html;
        index index.html;
}
EOF

##重新启动NGINX
systemctl restart nginx

#检查
ss -lnt|grep 80

#（6）测试游戏
打开浏览器，输入game.etiantian.org测试昨天的游戏网站
```

第3天：博客网站， [LNMP与LAMP](https://cloud.tencent.com/developer/article/1654099)  

```sh
#1）安装mariadb数据库**													# 安装mariadb，设密码
yum install mariadb mariadb-server -y
#成功标志，是“完毕”！ 一定要注意这一行的成功标志。

#2.启动mariadb服务
systemctl start mariadb
#成功标志，有一个短暂停顿，然后没有任何反应。

#3.设置开机自启动
systemctl enable mariadb
#成功标志，是开头有Created symlink字样

##4.执行mysql登录数据库
mysql
#成功标志,开头有MariaDB [(none)]>

##5.执行下面快捷键退出数据库，回到Linux环境下。
# ctrl+d 一次
#成功标志,回到[root@oldboyedu ~]# 

##6.给Mariadb配置登陆密码oldboy123，可以自己改，但学习期间尽量不要改。
mysqladmin password 'oldboy123'  ###不要改。。。
#成功标志是没有反应。

##7.指定用户root，并用新密码oldboy123进行登录数据库
mysql -uroot -p'oldboy123'
#成功标志,开头有MariaDB [(none)]>

##8.登录成功后，开头有MariaDB [(none)]>,然后执行quit，退出数据库
或执行ctrl+d快捷键
#成功标志,回到[root@oldboyedu ~]# 


																		# 配置nginx
#1）检查nginx+php+mariadb 3个服务是否启动
ss -lnt|egrep "80|9000|3306"
#成功标志，含有9000和80、3306红色字样。

#2.生成博客blog虚拟主机配置文件
cat >/etc/nginx/conf.d/03_blog.etiantian.org.conf<<'EOF'
server {
        server_name blog.etiantian.org;  ## 博客域名配置。
        listen 80;                       ## 网站的端口。
        root /data/blog;                 ## 程序目录。
        index index.php index.html;      ## 首页文件。
    location ~ \.php$ {                  ## 以.php结尾的所有文件都发给PHP服务（9000）处理。     
        fastcgi_pass   127.0.0.1:9000;   ##把PHP程序抛给PHP服务处理。
        fastcgi_index  index.php;
        fastcgi_param  SCRIPT_FILENAME  $document_root$fastcgi_script_name;
        include        fastcgi_params;
       }
    }
EOF
cat /etc/nginx/conf.d/03_blog.etiantian.org.conf

#3.检查nginx配置语法，并重启
#检查语法
nginx -t
##成功标志 结尾successful

#重启nginx服务
systemctl restart nginx
##成功标志 没有反应

#4.创建博客目录，并切到博客目录下
mkdir -p /data/blog && cd /data/blog  ##成功标志 没有反应

#5将群里wordpress-4.9.6.zip软件下载下来，不要解压								# 安装博客
#6.将下载的软件拖动到xshell里linux窗口，然后执行ls检查。
#7.解压博客程序
unzip -o wordpress*.zip

#8.把解压后的wordpress目录里面的程序挪到目录外面
mv wordpress/* /data/blog/
##成功标志 没有反应

#9.授权nginx服务和PHP服务得对应nginx用户管理博客目录/data/blog
chown -R nginx /data/blog/							# 设置目录下的所有文件与子目录的拥有者为 nginx
##成功标志 没有反应

10.打开谷歌浏览器输入blog.etiantian.org，回车访问。具体安装步骤 见视频
欢迎使用WordPress。在开始前，我们需要您数据库的一些信息。请准备好如下信息。
数据库名
数据库用户名
数据库密码
数据库主机
数据表前缀（table prefix，特别是当您要在一个数据库中安装多个WordPress时）
我们会使用这些信息来创建一个wp-config.php文件。 如果自动创建未能成功，不用担心，您要做的只是将数据库信息填入配置文件。您也可以在文本编辑器中打开wp-config-sample.php，填入您的信息，并将其另存为wp-config.php。 需要更多帮助？看这里。
绝大多数时候，您的网站服务提供商会给您这些信息。如果您没有这些信息，在继续之前您将需要联系他们。如果您准备好了…

db:test root oldboy123
```

