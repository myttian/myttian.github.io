# 5、 [bx]和loop

**小结**： ==[bx]==`偏移地址在bx中的内存单元` ， debug和汇编程序对指令的不同处理，  `内存单元ffff:0006中的数乘以3， 结果存储在dx中`, 



- `mov ax,[0]` 只是把偏移地址0传过去，不是传0对应的内存单元传过去
  - debug和masm写的汇编指令，执行效果不一样，例：masm写的 `mov al,[1]`,用debug调试，实际是 `mov al,1`,而不是把偏移地址1的内容放入al

- ==[bx]==同样也表示一个内存单元，它的`偏移地址在bx中`
  - 描述内存单元，需要内存单元`地址和长度 (例如[2]和ax)`

- 取内容： 偷懒，用`()`表示寄存器或内存单元中的`内容`
- `idata`表示常量

| ![](./汇编5-10.assets/Snipaste_2024-08-04_13-32-23.jpg) | ![debug和masm写的汇编指令执行效果不同](./汇编5-10.assets/Snipaste_2025-02-28_11-05-17.jpg) |
| ------------------------------------------------------- | ------------------------------------------------------------ |
| ![](./汇编5-10.assets/Snipaste_2025-02-28_11-09-47.jpg) | ![先把常量0传到bx](./汇编5-10.assets/Snipaste_2025-03-02_11-02-23.jpg) |
| ![](./汇编5-10.assets/Snipaste_2024-08-04_13-38-34.jpg) | ![](./汇编5-10.assets/Snipaste_2025-02-28_14-51-37.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2024-08-04_15-16-21.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-04_15-17-02.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2024-08-04_15-17-41.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-04_15-18-23.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2024-08-04_15-19-09.jpg) | ![偏移地址是bx的内存单元,的内容](./汇编5-10.assets/Snipaste_2024-08-04_15-20-28.jpg) |
| ![](./汇编5-10.assets/Snipaste_2024-08-04_15-21-41.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-04_15-31-00.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2025-03-03_00-28-11.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-03_00-29-50.jpg)      |



```assembly
assume cs:codesg
codesg segment

fish:mov ax,2000H	
	 mov ds,ax
	 mov bx,1000H
	 mov ax,[bx]			;(ax) = 00BE
	 inc bx					;21001H
	 inc bx					;21002H
	 mov [bx],ax			;(21002) = 00BE
	 inc bx					;21003H
	 inc bx					;21004H
	 mov [bx],ax			;(21004) = 00BE
	 inc bx					;21005H
	 mov [bx],al			;(21005) = BE
	 inc bx
	 mov [bx],al			;(21006) = BE
	 
	 mov ax,4C00H
	 int 21H
	 
codesg ends
end
```

## loop

- CX==存循环次数==
- debug调试时循环不可能`t`很多次， `g 0014`直接执行完循环，`p` 也可以执行完循环
- 执行loop指令时，2步操作

  1. `(cx) = (cx) - 1`
  2. 判断cx中的值：不为0转至标号处执行, `(IP)=(IP)+8`，为0向下执行

| ![](./汇编5-10.assets/Snipaste_2024-08-04_15-45-25.jpg)      | ![](./汇编5-10.assets/Snipaste_2024-08-04_15-46-39.jpg)      |
| ------------------------------------------------------------ | ------------------------------------------------------------ |
| ![](./汇编5-10.assets/Snipaste_2024-08-04_18-03-52.jpg)      | ![](./汇编5-10.assets/Snipaste_2024-08-04_18-05-02.jpg)      |
| ![start设程序开始地址](./汇编5-10.assets/Snipaste_2025-02-28_15-10-07.jpg) | ![](./汇编5-10.assets/Snipaste_2025-02-28_15-12-46.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2025-03-03_14-51-05.jpg)      | ![](./汇编5-10.assets/Snipaste_2025-03-03_14-52-09.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2025-03-03_14-53-05.jpg)      | ![寄存器cx最开始存放程序的大小,000f](./汇编5-10.assets/Snipaste_2025-03-27_23-26-35.jpg) |

寄存器cx最开始存放程序的大小，例如15个字节

```assembly
assume cs:code
code segment

start:mov ax,2		;入口
	 mov cx,11
   s:add ax,ax
	 loop s
	 
	 mov ax,4C00H
	 int 21H
	 
code ends
end
```

习题： 内存单元内容赋值给ax，ax加3次，保存到dx中

- 内存单元ffff:0006中的数乘以3， 结果存储在dx中

要考虑到问题：

- 运算`结果存不下`
- 内存单元8位，用来计算的ax寄存器16位， `类型不一样`

程序第一行ax是0ffff, 因为汇编程序中，数据不能以字母开头，前面要加`0`

| ![](./汇编5-10.assets/Snipaste_2025-03-03_17-55-03.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-03_18-00-26.jpg)      |
| ------------------------------------------------------- | ------------------------------------------------------------ |
| ![](./汇编5-10.assets/Snipaste_2025-03-03_18-03-30.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-03_18-05-02.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2025-03-03_18-06-08.jpg) | ![程序第一行ax是0ffff](./汇编5-10.assets/Snipaste_2024-08-04_21-55-23.jpg) |
| ![](./汇编5-10.assets/Snipaste_2024-08-04_21-59-10.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-03_18-19-45.jpg)      |

## debug和汇编程序对指令的不同处理

汇编程序中 `mov ax,[0]`被当作 `mov ax,0`处理， ==要加段前缀== `mov ax,ds:[0]`

- bx  存偏移地址，与ds配合
- loop  cx循环次数
- 段前缀
- 一段安全的空间 `0：200 - 0：2FF` 256字节

| ![](./汇编5-10.assets/Snipaste_2025-03-03_18-29-16.jpg)      | ![](./汇编5-10.assets/Snipaste_2025-03-03_18-30-23.jpg)      |
| ------------------------------------------------------------ | ------------------------------------------------------------ |
| ![](./汇编5-10.assets/Snipaste_2025-03-03_18-31-55.jpg)      | ![](./汇编5-10.assets/Snipaste_2025-03-03_18-34-56.jpg)      |
| ![会识别错误，要把偏移地址放到bx中](./汇编5-10.assets/Snipaste_2025-03-03_18-36-12.jpg) | ![偏移地址放到bx中](./汇编5-10.assets/Snipaste_2025-03-03_18-39-16.jpg) |
| ![](./汇编5-10.assets/Snipaste_2024-08-04_22-13-14.jpg)      | ![](./汇编5-10.assets/Snipaste_2024-08-04_22-14-02.jpg)      |
| ![最后2个masm识别的一样](./汇编5-10.assets/Snipaste_2024-08-04_22-15-52.jpg) | ![]()                                                        |

## loop和[bx]联合应用

分析：

- 运算结果可能超出dx存储范围
- 内存单元8位，寄存器16位，类型不一样，不能直接加
- 如果内存单元加到dl中，连加12个8位数据，有可能越界数据溢出，造成进位丢失

==解决办法==：8位数据赋值到用1个16位寄存器ax做中介,再用ax加到dx寄存器； `mov al,ds:[0]   mov ah,0` 

| ![](./汇编5-10.assets/Snipaste_2025-03-02_01-27-00.jpg) | ![数据类型不同不能直接累加](./汇编5-10.assets/Snipaste_2025-03-02_01-27-56.jpg) |
| ------------------------------------------------------- | ------------------------------------------------------------ |
| ![](./汇编5-10.assets/Snipaste_2025-03-02_01-28-36.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-02_01-29-04.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2024-08-05_09-26-12.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-02_01-31-14.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2025-03-02_01-32-49.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-02_01-36-41.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2025-03-02_01-37-52.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-02_01-38-18.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2025-03-02_01-39-11.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-02_01-39-59.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2025-03-02_01-40-33.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-02_01-41-09.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2025-03-02_01-44-53.jpg) | ![]()                                                        |

## 段前缀

`mov ax,cs:[1]` 

| ![](./汇编5-10.assets/Snipaste_2025-03-02_01-48-17.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-05_09-33-03.jpg) |
| ------------------------------------------------------- | ------------------------------------------------------- |

## 一段安全的空间
| ![](./汇编5-10.assets/Snipaste_2025-03-02_01-51-16.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-05_09-34-59.jpg) |
| ------------------------------------------------------- | ------------------------------------------------------- |

## 段前缀的使用

`内存单元的12个字节，拷贝到另一段内存单元`

解决： 源数据放入dl，再把dl数据mov到目标地址

优化： 源数据段地址放入ax，目标数据段地址放入es  `mov es:[bx],dl` 

| ![](./汇编5-10.assets/Snipaste_2024-08-05_11-33-11.jpg)      | ![](./汇编5-10.assets/Snipaste_2025-03-04_15-13-11.jpg)      |
| ------------------------------------------------------------ | ------------------------------------------------------------ |
| ![](./汇编5-10.assets/Snipaste_2025-03-04_15-14-47.jpg)      | ![bx做变量，dl做中转](./汇编5-10.assets/Snipaste_2024-08-05_11-32-29.jpg) |
| ![程序优化](./汇编5-10.assets/Snipaste_2025-03-04_15-27-31.jpg) | ![优化,循环里不用重复设置ds](./汇编5-10.assets/Snipaste_2024-08-05_11-37-20.jpg) |
| ![](./汇编5-10.assets/Snipaste_2024-08-05_11-38-12.jpg)      |                                                              |



    5.1 [bx]
        1.和[0]类似，[0]表示内存单元，它的偏移地址是0；
        2.[bx]同样也表示一个内存单元，它的段地址在DS中
            它的偏移地址在bx中，至于是取字还是取字节，
            要看他放入的寄存器是8位还是16位
        3.补充：inc指令：相当于C语言中的++运算符
    5.2 Loop指令
        这个指令和循环有关
        1.指令格式：loop 标号
            CPU执行loop指令的时候，要进行两步操作
            1.(cx)=(cx)-1;
            2.判断cx中的值，若不为零，则转至标号处执行程序
                若为零，则向下执行。
        2.通常，loop指令实现循环，cx中存放循环的次数
        3.标号
            在汇编语言中，标号代表了一个地址，标号标识了一个地址
        4.使用cx和loop指令相配合实现循环功能的三个要点
            1.在cx中存放循环次数
            2.loop指令中的标号所标识地址要在前面
            3.要循环执行的程序段，要写在标号和loop指令的中间
        5.用cx和loop指令相配合实现循环功能的程序框架
            mov cx,循环次数
          S:循环执行的程序段
            loop s
    5.3 在Debug中跟踪供loop指令实现的循环程序
        **注意：在汇编程序中，数据不能以字母开头，如果要输入像FFFFH这样的数
            则要在前面添加一个0
        在debug程序中引入G命令和P命令
        1.G命令
            G命令如果后面不带参数，则一直执行程序，直到程序结束
            G命令后面如果带参数，则执行到ip为那个参数地址停止
        2.P命令
            T命令相当于单步进入（step into）
            P命令相当于单步通过（step over）
    5.4 Debug和汇编编译器Masm对指令的不同处理
        1.在debug中，可以直接用指令 mov ax,[0] 将偏移地址为0号单元的内容赋值给ax
        2.但通过masm编译器，mov ax,[0] 会被编译成 mov ax,0
            1.要写成这样才能实现：mov ax,ds:[0]
            2.也可以写成这样：
                mov bx,0
                mov ax,[bx]  ;或者mov ax,ds:[bx]
    5.5 loop和[bx]的联合应用
        1.计算ffff:0~ffff:b单元中的数据的和，结果存储在dx中
            1.注意两个问题
                1.12个8位数据加载一起，最后的结果可能会超出8位（越界），故要用16位寄存器存放结果
                2.将一个8位的数据加入到16位寄存器中，类型不匹配，8位的数据不能与16位相加
            2.【解决办法】
                把原来8位的数据，先通过通用寄存器ax，将它们转化成16位的
            3.代码如下
```
assume cs:codesg

codesg segment
start:
	;指定数据段
	mov ax,0ffffh
	mov ds,ax
	
	;初始化
	mov ax,0
	mov dx,0
	mov bx,0
	
	;指定循环次数，12次
	mov cx,0ch
circ:
	;把8位数据存入al中,即ax中存放的是[bx]转化之后的16位数据，前8位都是0
	mov al,[bx]
	;进行累加
	add dx,ax
	;bx自增，变化内存的偏移地址
	inc bx
	loop circ
	
	;程序返回
	mov ax,4c00h
	int 21H
codesg ends

end start    
```
    5.6 段前缀
        1.指令“mov ax,[bx]”中，内存单元的偏移地址由bx给出，而段地址默认在ds中
        2.我们可以在访问内存单元的指令中显式地给出内存单元的段地址所在的段寄存器
            比如  mov ax,ds:[0]
                  mov ax,ds:[bx]
            这里的ds就叫做【段前缀】
    5.7 一段安全的空间
        1.8086模式中，随意向一段内存空间写入内容是很危险的
            因为这段空间中可能存放着【重要的系统数据或代码】
        2.在一般的PC机中，DOS方式下，DOS和其他合法的程序一般都不会使用【0:200~0:2FF】
            的256个字节的空间。所以，我们使用这段空间是安全的    
# 6、 包含多个段的程序

[mangling是什么含义](https://blog.csdn.net/qiaoquan3/article/details/127771722)   [MASM32快速起步](https://www.cnblogs.com/Zblogs/p/3265570.html) 

## 代码段中使用数据

- 第一个例子：代码段前面16字节是数据，==指令从数据后面开始==, 所以需要定义程序入口，指明程序从哪里开始读指令
- cpu从end后面的标号找程序的入口

![](./汇编5-10.assets/Snipaste_2024-08-05_11-40-22.jpg)

- 因为cs段前面存的数据，要定义程序的入口， `start:`
- 入口： 代码段的第一条指令

| ![](./汇编5-10.assets/Snipaste_2025-03-05_01-17-39.jpg)      | ![](./汇编5-10.assets/Snipaste_2025-03-05_01-17-57.jpg)      |
| ------------------------------------------------------------ | ------------------------------------------------------------ |
| ![](./汇编5-10.assets/Snipaste_2025-03-05_01-19-37.jpg)      | ![](./汇编5-10.assets/Snipaste_2025-03-05_01-20-10.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2025-03-05_01-21-02.jpg)      | ![定义数据](./汇编5-10.assets/Snipaste_2025-03-05_01-23-55.jpg) |
| ![段地址](./汇编5-10.assets/Snipaste_2025-03-05_01-25-24.jpg) | ![偏移地址](./汇编5-10.assets/Snipaste_2025-03-05_01-26-11.jpg) |
| ![](./汇编5-10.assets/Snipaste_2025-03-05_01-27-22.jpg)      | ![代码段前面16字节是数据，指令从数据后面开始](./汇编5-10.assets/Snipaste_2024-08-05_12-24-12.jpg) |
| ![](./汇编5-10.assets/Snipaste_2024-08-05_12-27-30.jpg)      | ![](./汇编5-10.assets/Snipaste_2024-08-05_12-28-48.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2024-08-05_12-31-03.jpg)      | ![]()                                                        |



```assembly
assume cs:code
code segment
    dw 0124h,0456h,0789h,0abch,0defh,0fedh,0cbah,0987h
    ;代码段前面16字节是数据，指令从数据后面开始, 所以需要定义程序入口，指明程序从哪里开始读指令。
start:mov bx,0		;初始化bx,因为代码段开始是数据，用start指明程序入口
	mov ax,0		;初始化
	mov cx,8
  s:add ax,cs:[bx]	;偏移地址从0开始，8字节数据相加
    add bx,2
	loop s
	
	mov ax,4c00h
	int 21h
	
code ends
end start			；cpu从end后面的标号找程序的入口
```

## 代码段中使用栈

- 习题：数据逆序存放，要用到栈
- 设栈段，入栈出栈

| ![](./汇编5-10.assets/Snipaste_2025-03-05_12-55-12.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-05_12-57-27.jpg)      |
| ------------------------------------------------------- | ------------------------------------------------------------ |
| ![](./汇编5-10.assets/Snipaste_2025-03-05_12-58-31.jpg) | ![设栈段，入栈出栈,内存单元从0开始，栈最底地址是31](./汇编5-10.assets/Snipaste_2024-08-05_15-21-10.jpg) |
| ![](./汇编5-10.assets/Snipaste_2025-03-05_13-09-46.jpg) |                                                              |

## ==数据、代码、栈放入不同的段==

[实验3 （编程、编译、连接、跟踪） - 知乎](https://zhuanlan.zhihu.com/p/616804294)	

| ![](./汇编5-10.assets/Snipaste_2025-03-05_13-14-52.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-05_13-16-30.jpg) |
| ------------------------------------------------------- | ------------------------------------------------------- |
| ![](./汇编5-10.assets/Snipaste_2024-08-05_15-25-02.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-05_15-27-14.jpg) |
| ![](./汇编5-10.assets/Snipaste_2024-08-05_15-41-56.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-05_15-31-58.jpg) |
| ![](./汇编5-10.assets/Snipaste_2024-08-05_15-35-38.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-05_15-38-50.jpg) |
| ![](./汇编5-10.assets/Snipaste_2024-08-05_15-39-31.jpg) |                                                         |



    6.1在代码段中使用数据
        1.dw的含义【定义字型数据：define word，16字节】
            在数据段中使用dw定义数据，则数据在数据段中
            在代码段中使用dw定义数据，则数据在代码段中
            堆栈段也是一样
        2.在程序的第一条指令前加一个标号start，并且这个标号在伪指令end后面出现
            可以通知编译器程序在什么地方结束，并且也可以通知编译器程序的入口在哪里
    6.2在代码段中使用栈
        **补充：如果题目要求【逆序】存放，就要想到栈（FILO）
        使用dw向系统申请一段空间，然后把这个空间当做栈
    6.3将数据、代码、栈放入不同的段
        1.在前面的6.1和6.2中，我们在程序中用到了数据和栈，我们在编程的时候要注意
            何处是数据，何处是栈、何处是代码
        2.这样做显然有两个问题
            1.把他们放在一个段中是程序显得混乱
            2.前面程序中处理的数据很少，用到的栈空间也小，放在一个段里面没有问题
                但数据、栈、代码需要的空间超过64KB，就不能放在一个段中
                (8086中一个段的容量不能大于64KB)
        3.我们可以和定义代码段一样的方法来定义多个段
            然后在这些段里面定义需要的数据，或通过定义数据来取得栈空间
        4.将数据、代码、栈放入不同的段
            1.我们可以在源程序中为这三个段起具有含义的名称
                用来存放数据的段，我们将其命名为“data”
                用来存放代码的段，我们将其命名为“code”
                用来作栈空间的段，我们将其命名为“stack”
              但是CPU看得懂吗？【不能】
            2.我们在源程序中用伪指令
                “assume cs:code,ds:data,ss:stack”将cs、ds和ss分别和code、data、stack段相连
                这样做了之后，CPU是都就会将cs指向code，ds指向data，ss指向stack
                  从而按照我们的意图来处理这些段呢？【不能】
                  伪指令CPU看不懂，伪指令是给编译器看的
            3.若要CPU按照我们的安排行事，就要用机器指令控制它，源程序中的汇编指令
                才是CPU要执行的内容
                需在在code段中给DS，CS、SS设置相应的值才能让CPU识别出数据段、代码段、堆栈段
                其中汇编程序开始的地方（即代码段开始的地方）由end后面的标号所指向的地方给出
        5.assume指令不可省略，至于为什么，需要以后多多体会
    【实验五】
        1.如果段中的数据占N个字节，则程序加载后，这段实际占有的空间为：N%16==0?N:16×(N/16+1);
            因为一个段最小占用16字节，即有16个字节只有这个段可以访问到
        2.在编辑源程序的时候，如果调换各个段的编写位置，最后CS、DS、SS的值会发生变化
        3.如果去掉start，编译器会从上到下执行，如果第一个段是代码段，则可以正常运行
            若第一个段不是代码段，则不会正常运行
        4.代码示例1
```assembly
assume cs:code,ds:data,ss:stack

;数据段
data segment
    ;8个数据
	dw 0123h,0456h,0789h,0abch,0defh,0fedh,0cbah,0987h
data ends

;栈段
stack segment
	;8个数据
    dw 0,0,0,0,0,0,0,0
stack ends

;代码段
code segment
start:
	;栈空间初始化
	mov ax,stack
	mov ss,ax
	mov sp,16
	
	;数据段初始化
	mov ax,data
	mov ds,ax
	
	push ds:[0];一个栈单元是一个字
	push ds:[2]
	;存放数据不会改变
	pop ds:[2]
	pop ds:[0]
	
	;程序返回
	mov ax,4c00h
	int 21h
code ends
end 
```
        5.将a，b数据段中的内容分别相加，结果放入data数据段中
```
assume cs:code

;数据段
a segment
	db 1,2,3,4,5,6,7,8
a ends

;数据段
b segment
	db 1,2,3,4,5,6,7,8
b ends

;数据段
data segment
	db 0,0,0,0,0,0,0,0
data ends

;代码段
code segment
start:
	mov bx,0
	mov ax,0
	
	mov dx,a
	mov ss,dx
	
	mov dx,b
	mov es,dx
	
	mov dx,data
	mov ds,dx

	mov cx,8
circ:
	add al,ss:[bx]
	add al,es:[bx]
	mov [bx],al
	inc bx
	mov al,0
	loop circ
	
	;程序返回
	mov ax,4c00h
	int 21h
code ends
end start
```
        6.将a数据段中的前8个字型数据逆序存储到b段中
```
assume cs:code
a segment
	dw 1,2,3,4,5,6,7,8,9,0ah,0bh,0ch,0dh,0eh,0fh,0ffh
a ends

b segment
	dw 0,0,0,0,0,0,0,0
b ends

code segment
start:
    mov ax,0
	mov ax,a
	mov ss,ax
	mov sp,0
	
	mov ax,0
	mov ax,b
	mov ds,ax
	
	mov bx,0
	
	mov cx,8
circ:
	pop [bx]
	add bx,2
	loop circ

	mov ax,4c00h
	int 21h
code ends
end start 
```
# 7、 更灵活地==定位内存地址==

## and和or大小写转换

- and逻辑与：全真为真
  - `mov al,011B;   and al,111` 

- 单引号表示字符  `'a'` 
- 大小写转换：改变对应的ASCII码, `小写 -20h = 大写`
  - 20h二进制是`10 0000`,第6位是1，将小写第6位变为0，就转为大写


|   ![](./汇编5-10.assets/Snipaste_2024-08-05_15-46-15.jpg)    | ![](./汇编5-10.assets/Snipaste_2025-03-05_16-53-16.jpg) |
| :----------------------------------------------------------: | :-----------------------------------------------------: |
|   ![](./汇编5-10.assets/Snipaste_2025-03-05_16-53-55.jpg)    | ![](./汇编5-10.assets/Snipaste_2025-03-05_16-56-05.jpg) |
|   ![](./汇编5-10.assets/Snipaste_2025-03-05_16-57-02.jpg)    | ![](./汇编5-10.assets/Snipaste_2025-03-05_16-57-34.jpg) |
|   ![](./汇编5-10.assets/Snipaste_2025-03-05_16-59-38.jpg)    | ![](./汇编5-10.assets/Snipaste_2025-03-05_17-00-24.jpg) |
|   ![](./汇编5-10.assets/Snipaste_2025-03-05_17-01-04.jpg)    | ![](./汇编5-10.assets/Snipaste_2025-03-05_17-02-23.jpg) |
| ![大写A是41H，小写a是61H](./汇编5-10.assets/Snipaste_2024-08-05_17-16-03.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-05_17-04-06.jpg) |
|   ![](./汇编5-10.assets/Snipaste_2025-03-05_17-04-40.jpg)    | ![](./汇编5-10.assets/Snipaste_2025-03-05_17-06-42.jpg) |

## ==七种寻址方式==

[七种寻址方式](https://blog.csdn.net/u012928324/article/details/69334931)	 [好：七种寻址](https://www.cnblogs.com/liujy2233/p/17289478.html)  [好：指令的寻址方式](https://zhuanlan.zhihu.com/p/12104232425)	[好：内存寻址方式](https://blog.csdn.net/2301_80035097/article/details/138049856)	 

[好：寻址方式](https://blog.csdn.net/m0_59298585/article/details/125854499)  	[[bx + si]](https://www.cnblogs.com/HuiPuKui/p/17371049.html)	[定位内存地址的方法](https://cloud.tencent.com/developer/article/2206609)	

[寄存器寻址_百度搜索](https://www.baidu.com/s?ie=UTF-8&wd=%E5%AF%84%E5%AD%98%E5%99%A8%E5%AF%BB%E5%9D%80)	

[PCIe BAR](https://zhuanlan.zhihu.com/p/678143630)	

**EA偏移地址**‌，也称有效地址（Effective Address）; 物理地址（PA）; SA段地址

> 基址（base）：存放在基址寄存器（如BX、BP）中的内容。有效地址中的基址部分，通常用于指向数据段中数组或字符串的首地址
>
> 
>
> 变址（index）：存放在变址寄存器（如SI、DI）中的内容。通常用来==指向数组中某个元素==或字符串的某个字符
>
> 
>
> `EA = 基址＋（变址x比例因子）＋位移量`
>
> 
>
> #### 相对寻址: 相对一个固定位置做偏移
>
> - 用于结构体：`[bx].idata`
> - 用于数组：`idata[si],idata[di]`
> - 用于二维数组：`[bx][idata]` 
>
> #### 基址变址：两个变量
>
> - 二维数组 :`[bx][si]`
>
> #### 相对基址变址
>
> - 二维数组：`idata[bx][si]`
> - 结构中的数组项 `[bx].idata[si]`
>
> ==[idata]== 用一个常量来表示地址，可用于直接定位一个内存单元
> ==[bx]== 用一个变量来表示内存地址，可用于间接定位一个内存单元
> ==[bx + idata]== 用一个变量和常量表示地址，可在一个起始地址的基础上用变量间接定位一个内存单元
> ==[bx + si]== 用两个变量表示地址
> ==[bx + si + idata]== 用两个变量和一个常量表示地址 
>
> 
>
> **习题**
>
> - `[bx+idata]`:	单词首字母大写， bx加常量   `1.file`
> - `[bx+si]`：      每个单词大写，2个变量        `ibm`,    ==4行16列的二维数组== : ==2个循环== ,用2个变量
> - `[bx+si+idata]`：每个单词前4个字母大写，      `1.display`  





- 立即寻址	   `MOV AX,1234H`
- 寄存器寻址	 `MOV AX,BX`
- 直接寻址       `MOV　AX, [1234H]`
- 寄存器间接寻址  `MOV AX , [ BX ]`
- 寄存器相对寻址  `mov ax,12[bx]`  
- 基址加变址      `MOV BX, [BX+SI]`     `OV AX, [BX][DI]`
- 相对基址加变址   `MOV AX, [BX+SI+200H]` 



1. 立即寻址
   1. 操作数包含在指令中,	`MOV AX,1234H`
2. 寄存器寻址
   1. 操作数在CPU寄存器中,	`MOV AX,BX`		
3. **直接寻址**
   1. 操作数在内存中，指令中直接给出该操作数的有效地址.   ds加偏移地址
   2. 物理地址: 由==DS==和指令中给出的有效地址形成，但如果使用段超越前缀，操作数可存放在其它段
   3. 立即寻址和直接寻址书写格式不同，直接寻址的地址要写在括号“[”，“]”内。在程序中，直接地址通常用内存变量名来表示，如：`MOV BX, VARW`，其中，VARW是内存字变量
   4. 汇编语言指令中，可以用符号地址代替数值地址,  `MOV AX,[VALUE]`
   5. `MOV　AX, 1234H     MOV　AX, [1234H]` ;  前者是立即寻址，后者是直接寻址 
4. 寄存器间接寻址
   1. `MOV AX , [ BX ]`
   2. 不允许使用AX、CX、DX 存放 EA ,  有效地址只能在SI、DI、BX、BP中
5. **寄存器相对寻址** 
   1. 有效地址是一个基址寄存器(BX、BP)或变址寄存器(SI、DI)的内容 加上 指令中给定的8位或16位位移量之和
   2. 操作数的有效地址: `基址或变址寄存器内容` 和 指令中指定的位`偏移量`之和
   3. `MOV AX,[DI+1223H]`     `MOV AX , COUNT [ SI ]`    `mov ax,12[bx]`
6. 基址加变址
   1. 有效地址是一个基址寄存器（如BX、BP）和一个变址寄存器（如SI、DI）的内容之和。 有效地址由两种成分组成
   2. `MOV AX, [BX][DI]`      `MOV AX, [BX+DI]`     `MOV AX, ES:[BX][SI]`
7. 相对基址加变址
   1. 操作数有效地址是一个`基址寄存器`（如BX、BP）的值与一个`变址寄存器`（如SI、DI）的值 和 `指令中的偏移量`之和。（ 16位寻址位：移量为8位/16位） 有效地址由三种成分组成
   2. `MOV AX, [BX+SI+200H]` 

| ![直接寻址](./汇编5-10.assets/Snipaste_2025-03-07_14-01-50.jpg) | ![](./汇编5-10.assets/reg.jpg)                          |
| ------------------------------------------------------------ | ------------------------------------------------------- |
| ![](./汇编5-10.assets/re.png)                                | ![](./汇编5-10.assets/Snipaste_2025-03-07_15-17-53.jpg) |
| ![相对基址加变址](./汇编5-10.assets/Snipaste_2025-03-07_15-21-40.jpg) |                                                         |



### 基址寄存器和变址寄存器

[好：基址寄存器和变址寄存器的区别](https://docs.pingcode.com/ask/316471.html)	

[变址寄存器_百度百科](https://baike.baidu.com/item/%E5%8F%98%E5%9D%80%E5%AF%84%E5%AD%98%E5%99%A8/7680616)	[寄存器寻址方式_百度百科](https://baike.baidu.com/item/%E5%AF%84%E5%AD%98%E5%99%A8%E5%AF%BB%E5%9D%80%E6%96%B9%E5%BC%8F/6686408?fr=aladdin)	[8086微处理器中的寄存器类型](https://baijiahao.baidu.com/s?id=1824746587385594030&wfr=spider&for=pc)	[好：寄存器有哪几种?可以实现什么功能? ](https://design.eccn.com/design_2024120610463922.htm)	 

1. 14个16位[寄存器](https://baike.baidu.com/item/寄存器/187682?fromModule=lemma_inlink)，8个8位寄存器，7种[寻址方式](https://baike.baidu.com/item/寻址方式/3210621?fromModule=lemma_inlink) 
2. 基址寄存器 （Base Register）
   1. `BX`
   2. 通常用于程序的多任务处理和内存保护。它包含一个特定的地址，通常是一段`内存的起始地址`
   3. CPU访问内存时，会将基址寄存器中的地址与指令中给出的偏移量相加，形成最终的物理地址
   4. 这样做的好处在于，可以实现程序的位置无关代码，因为无论程序实际加载到内存中的什么位置，程序指令中使用的都是相对地址
3. 变址寄存器（Index Register）
   1. `ESI、EDI、SI和DI`，存放存储单元在`段内的偏移量`
   2. 用途：SI：存储器指针，串指令（`字符串操作`）中的源操作数指针；   DI：存储器指针，串指令中的目的操作数指针
   3. 主要用于==处理数据结构==中的元素，如数组。它==存储的是一个索引值==，CPU可以将这个索引值与指令中给出的基础地址相加，来形成数组中每个元素的物理地址
   4. 相较于基址寄存器，变址寄存器的值在执行指令期间可以动态变化，对于执行循环和迭代计算非常有用

## [bx+idata]数组处理

- idata: 常数
  - 偏移地址10h拆分为(bx)+2

- `[bx+1]` 偏移地址加1, 处理数组
  - `bx`不用递增`1`, 获取下一个内存单元的地址


| ![偏移地址10h拆分为(bx)+2](./汇编5-10.assets/Snipaste_2024-08-05_17-20-29.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-05_17-22-23.jpg)      |
| ------------------------------------------------------------ | ------------------------------------------------------------ |
| ![](./汇编5-10.assets/Snipaste_2024-08-05_17-23-18.jpg)      | ![bx不用递增1, 获取下一个内存单元的地址](./汇编5-10.assets/Snipaste_2025-03-05_17-24-25.jpg) |
| ![](./汇编5-10.assets/Snipaste_2025-03-05_17-28-34.jpg)      | ![](./汇编5-10.assets/Snipaste_2024-08-05_17-26-42.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2025-03-05_17-32-47.jpg)      | ![](./汇编5-10.assets/Snipaste_2025-03-05_17-34-21.jpg)      |

### 数组处理

- bx相当于i，`i++`等于`inc bx` 

| ![](./汇编5-10.assets/Snipaste_2025-03-05_17-37-04.jpg)      | ![](./汇编5-10.assets/Snipaste_2025-03-05_17-38-13.jpg)      |
| ------------------------------------------------------------ | ------------------------------------------------------------ |
| ![](./汇编5-10.assets/Snipaste_2025-03-05_17-39-25.jpg)      | ![bx相当于i，i++等于inc bx](./汇编5-10.assets/Snipaste_2025-03-05_17-42-40.jpg) |
| ![](./汇编5-10.assets/Snipaste_2024-08-05_17-36-52.jpg)      | ![bx相当于i，i++等于inc bx](./汇编5-10.assets/Snipaste_2024-08-05_17-38-57.jpg) |
| ![bx相当于i，i++等于inc bx](./汇编5-10.assets/Snipaste_2024-08-05_17-39-26.jpg) |                                                              |

## ==SI、DI==

- 源地址 目标地址
- `[bx+si]、[bx+di]`， 2个变量相加
- `[bx+si+idata]、[bx+di+idata]`

| ![](./汇编5-10.assets/Snipaste_2024-08-06_16-23-21.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-07_11-31-07.jpg)      |
| ------------------------------------------------------- | ------------------------------------------------------------ |
| ![](./汇编5-10.assets/Snipaste_2025-03-07_11-31-43.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-07_13-05-20.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2025-03-07_13-06-11.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-06_16-25-39.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2024-08-06_16-27-09.jpg) | ![优化：用数组的思维](./汇编5-10.assets/Snipaste_2024-08-06_16-28-14.jpg) |

### [bx+si]

2个变量, `[bx+idata]`中的常量换成变量

| ![2个变量相加](./汇编5-10.assets/Snipaste_2024-08-06_16-29-22.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-06_16-30-04.jpg) |
| ------------------------------------------------------------ | ------------------------------------------------------- |
| ![](./汇编5-10.assets/Snipaste_2024-08-06_16-30-39.jpg)      | ![](./汇编5-10.assets/Snipaste_2025-03-09_11-05-52.jpg) |
| ![](./汇编5-10.assets/Snipaste_2025-03-09_11-07-46.jpg)      | ![](./汇编5-10.assets/Snipaste_2025-03-09_11-11-00.jpg) |

### [bx+si+idata]
| ![](./汇编5-10.assets/Snipaste_2024-08-06_16-39-00.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-09_11-14-53.jpg) |
| ------------------------------------------------------- | ------------------------------------------------------- |
| ![](./汇编5-10.assets/Snipaste_2024-08-06_16-40-23.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-06_16-44-22.jpg) |

## 不同寻址方式的应用

- cpu提供多种寻址方式的用意
- `[bx+idata] `   :单词首字母大写， bx加常量
- `[bx+si]` 每个单词大写，2个变量

### [bx+idata]

| ![](./汇编5-10.assets/Snipaste_2024-08-06_16-46-36.jpg)     | ![](./汇编5-10.assets/Snipaste_2024-08-06_16-47-07.jpg) |
| ----------------------------------------------------------- | ------------------------------------------------------- |
| ![字母](./汇编5-10.assets/Snipaste_2025-03-09_16-44-58.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-06_16-48-57.jpg) |
| ![](./汇编5-10.assets/Snipaste_2025-03-09_16-49-59.jpg)     | ![](./汇编5-10.assets/Snipaste_2024-08-06_16-51-06.jpg) |
| ![](./汇编5-10.assets/Snipaste_2024-08-06_16-52-00.jpg)     | ![](./汇编5-10.assets/Snipaste_2024-08-06_16-53-14.jpg) |

### ==二维数组`[bx][si]`==

**习题**

- `[bx+idata]`:	单词首字母大写， bx加常量   `1.file`
- `[bx+si]`：      每个单词大写，2个变量        `ibm`,    ==4行16列的二维数组== : ==2个循环== ,用2个变量
- `[bx+si+idata]`：每个单词前4个字母大写，      `1.display`  

每行要执行3次与运算: ==2个循环==，外循环改变行，内循环改变每行的3个字母

- ==4行16列的二维数组== : ==2个循环== ,用2个变量

| ![单词](./汇编5-10.assets/Snipaste_2025-03-09_16-54-44.jpg)  | ![](./汇编5-10.assets/Snipaste_2025-03-09_16-57-32.jpg) |
| ------------------------------------------------------------ | ------------------------------------------------------- |
| ![](./汇编5-10.assets/Snipaste_2025-03-09_16-59-57.jpg)      | ![](./汇编5-10.assets/Snipaste_2025-03-09_17-02-34.jpg) |
| ![](./汇编5-10.assets/Snipaste_2025-03-09_17-04-05.jpg)      | ![](./汇编5-10.assets/Snipaste_2024-08-06_16-58-11.jpg) |
| ![2个变量](./汇编5-10.assets/Snipaste_2024-08-06_17-00-57.jpg) |                                                         |



```assembly
assume cs:codesg,ds:datasg
datasg segment
db 'ibm             '
db 'dec             '
db 'dos             '
db 'vax             '
datasg ends

codesg segment
start:mov ax,datasg
	  mov ds,ax
	  mov bx,0
	  
	  mov cx,4			;4行
   s0:mov si,0
      mov cx,3			;3列
    s:mov al,[bx+si]
      and al,11011111b
      mov [bx+si],al
      
      inc si
      loop s			;此时3次内循环完，cx为0
     
      add bx,16	 
	  loop s0			；cx=cx-1, 0-1，cx就变成ffff，溢出
	  
	  mov ax,4c00h
	  int 21h
	  
codesg ends
end start
```



### ==改进：用栈临时保存数据==

**问题**: 上面第二个例子，会出现死循环  

- 改进：用==dx==临时保存cx数据
- 再次改进：用==内存空间==临时保存数据
- 再再次改进：用==**栈**==

调用函数时，要把当前寄存器保存起来，调用完后再恢复当前寄存器数据

| ![只能用一个cx计数](./汇编5-10.assets/Snipaste_2025-03-09_17-15-37.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-09_17-17-34.jpg)      |
| ------------------------------------------------------------ | ------------------------------------------------------------ |
| ![会死循环](./汇编5-10.assets/Snipaste_2025-03-09_17-15-04.jpg) | ![进入内循环前cx暂时保存到dx中](./汇编5-10.assets/Snipaste_2025-03-09_17-18-30.jpg) |
| ![](./汇编5-10.assets/Snipaste_2025-03-09_17-20-46.jpg)      | ![](./汇编5-10.assets/Snipaste_2025-03-09_17-21-16.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2025-03-09_17-21-36.jpg)      | ![](./汇编5-10.assets/Snipaste_2025-03-09_17-22-04.jpg)      |
| ![再次改进：用内存空间临时保存数据](./汇编5-10.assets/Snipaste_2025-03-09_17-22-27.jpg) | ![cx值保存到内存](./汇编5-10.assets/Snipaste_2025-03-09_17-23-16.jpg) |
| ![](./汇编5-10.assets/Snipaste_2025-03-09_17-25-31.jpg)      | ![](./汇编5-10.assets/Snipaste_2025-03-09_17-25-52.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2025-03-09_17-26-40.jpg)      | ![调用函数时，要把当前寄存器保存起来，调用完后再恢复当前寄存器数据](./汇编5-10.assets/Snipaste_2025-03-09_17-34-15.jpg) |
| ![push](./汇编5-10.assets/Snipaste_2025-03-09_17-32-07.jpg)  | ![](./汇编5-10.assets/Snipaste_2025-03-09_17-33-35.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2025-03-09_17-35-00.jpg)      | ![push cx](./汇编5-10.assets/Snipaste_2025-03-09_17-35-52.jpg) |
| ![pop cx](./汇编5-10.assets/Snipaste_2025-03-09_17-36-22.jpg) |                                                              |

### ==`[bx+si+idata]`==

| ![](./汇编5-10.assets/Snipaste_2025-03-09_17-39-04.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-09_18-57-47.jpg) |
| ------------------------------------------------------- | ------------------------------------------------------- |
| ![](./汇编5-10.assets/Snipaste_2025-03-09_18-58-54.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-09_19-05-33.jpg) |
| ![](./汇编5-10.assets/Snipaste_2025-03-09_19-06-10.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-09_19-07-16.jpg) |
| ![](./汇编5-10.assets/Snipaste_2025-03-09_19-08-01.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-09_19-10-03.jpg) |
| ![](./汇编5-10.assets/Snipaste_2025-03-09_19-11-31.jpg) |                                                         |



    本章主要讲解一些更灵活的定位内存地址的方法和相关的编程方法
    7.1 and和or指令
        1.and指令：逻辑与指令，按位进行与运算
            1.如:mov al,01100011B
                 and al,00111011B
              执行后：
                 al=00100011B
            2.通过and指令可将操作对象的相应位设为0，其他位保持不变
                例如al的第6位设为0：and al,10111111B
                例如al的第7位设为0：and al,01111111B
                例如al的第0位设为0：and al,11111110B
        2.or指令，逻辑或运算，按位进行或运算
            1.如:mov al,01100011B
                 or  al,00111011B
              执行后：
                al=01111011B
            2.通过该指令可将操作对象的相应位设为1，其他位不变
                or al,01000000B;将al的第6位设为1
                or al,10000000B;将al的第7位设为1
                or al,00000001B;将al的第0位设为1
    7.2 关于ASCII码
        一种编码方案，在计算机系统中通常被采用，8位


    7.3 以字符形式给出的数据
        1.在汇编程序中，可以使用'×××'的方式指明数据是以字符的形式给出的
        2.编译器会将它们转化为相应的ASCII码
        3.例如
            1.db 'unIX'   ;相当于：db 75H,6EH,49H,58H
                'u'、'n'、'I'、'X'的ASCII码分别为75H,6EH,49H,58H
            2.mov al,'a'  ;相当于：mov al，61H
                'a'的ASCII码为61H
        4.ASCII码中，大写字母和小写字母之间的规律
            小写字母=大写字母+32
            小写字母=大写字母+20H
            大写字母从41H开始排，小写字母从61H开始排
大写|二进制|小写|二进制
-|-|-|-
A|01000001|a|01100001
B|01000010|b|01100010
C|01000011|c|01100011
D|01000100|d|01100100

    7.4 大小写转换的问题
        1.方案一：
            1.识别出是该字节是表示一个的大写英文字符，还是小写的
                用于条件判断的汇编程序，目前还没有学到
            2.根据+20H 或者 -20H进行大小写转换
        2.方案二：
            1.若全部转化为大写，则将第5位置0
                and al,11011111B
            2.若全部转化为小写，则将第5位置1
                or  al,00100000B
    7.5 [bx+常数]
        mov ax,[bx+200]的含义：
        1.将一个内存单元的内容送入ax，这个内存单元的长度为2字节，存放一个入一个子单元
          该字单元的偏移地址为bx中的数值加上200，段地址在ds中
        2.也可以写成
            1.mov ax,200[bx]
            2.mov ax,[bx].200
    7.6 用[bx+idata]的方式进行数组的处理
        在codesg中填写代码，将datasg中定义的第一个字符串转化为大写，第二个字符串转化为小写
          1.我们观察datasg段中的两个字符串，一个的起始地址为0，另一个的起始地址为5
          2.我们可以将这两个字符串看作两个数组，一个从0地址开始存放，另一个从5开始存放
          3.我们可以用[0+bx]和[5+bx]的方式在同一个循环中定位这两个字符串中的字符
          4.注意这个数组的定位方式，对比C语言
            C语言的数组定位方式：a[i]，b[i]，  a、b是地址常量
            汇编语言的数组定位方式：0[bx],5[bx]
            所以：[bx+常数]的方式为高级语言实现数组提供了便利的机制
```
assume cs:codesg,ds:datasg

datasg segment
	db 'BaSiC'
	db 'MinIX'
datasg ends

codesg segment
start:
	mov ax,datasg
	mov ds,ax
	mov bx,0
	
	mov cx,5	;做5次循环
circ:
	mov al,[bx]
	and al,11011111b
	mov [bx],al
	mov al,[bx+5];等价于mov al,5[bx];等价于mov al,[bx].5
	or al,00100000b
	mov 5[bx],al
	inc bx
	loop circ
	
	mov ax,4c00h
	int 21h
codesg ends
end start
```
    7.7 SI和DI
        已经学过的10个寄存器：AX、BX、CX、DX、DS、CS、SS、ES、IP、SP
        1.SI和DI是8086CPU中和bx功能相近的寄存器
            bx不够用，所以引进了SI和DI
        2.SI和DI（16位）不能够分成两个8位寄存器来使用【和bx的区别】
        3.下面三组指令实现了相同的功能
            1.mov bx,0
              mov ax,[bx]
            2.mov si,0
              mov ax,[si]
            3.mov di,0
              mov ax,[di]
        4.下面三组指令也实现了相同的功能
            1.mov bx,0
              mov ax,[bx+123]
            2.mov si,0
              mov ax,[si+123]
            3.mov di,0
              mov ax,[di+123]
        5.用寄存器SI和DI实现将字符串'welcome to masm!'复制到它后面的数据区中
            通常用ds:si指向要复制的源始字符串
            通常用ds:di指向要复制的目的空间
            **注意si、di是16位寄存器，循环中自增时，应该+2
```
assume cs:code,ds:data
data segment
	db 'welcome to masm!'
	db '................'
data ends

code segment
start:
	mov ax,data
	mov ds,ax
	mov si,0
	mov di,16
	
	mov cx,8
circ:
	mov ax,0[si]
	mov [di],ax
	inc di
	inc di
	inc si
	inc si
	loop circ
	
	mov ax,4c00h
	int 21h
code ends
end start
```
    7.8 [bx+si]和[bx+di]
        1.[bx+si]和[bx+di]的含义类似，我们以[bx+si]为例进行讲解
            [bx+si]表示一个内存单元，它的偏移地址为bx中的数值加上si中的数值
              它的偏移地址在ds中
        2.[bx+si]也可以写成[bx][si]
    7.9 [bx+si+常数]和[bx+di+常数]
        1.以[bx+Si+常数]为例讲解
            [bx+si+常量]表示一个内存单元，偏移地址为bx的值+si的值+常数
        2.指令mov ax,[bx+si+常数]也可以写成如下形式
            1.mov ax,200[bx+si]
            2.mov ax,200[bx][si]
            3.mov ax,[bx].200[si]
    7.10 不同的寻址方式的灵活应用
        1.总结几种定位内存的方法
            1.ds:[常数]   【直接寻址】
                用一个常量来表示地址，可用于直接定位一个内存单元
            2.[bx]      【寄存器间接寻址】
                用一个寄存器的值来表示内存地址，可以间接定位一个内存单元
            3.[bx+常数]   【？？】
                用一节寄存器的值和常量表示内存地址，可在一个起始地址的基础上用变量间接定位一个内存单元
            4.[bx+si]
            5.[bx+si+常数]
        2.编程，给定数据段data，将data段中每个单词的头一个字母改写成大写字母
```
assume cs:code,ds:data
data segment
	db '1. file         '
	db '2. edit         '
	db '3. search       '
	db '4. view         '
	db '5. options      '
	db '6. help         '
data ends

code segment
start:
	mov ax,data
	mov ds,ax
	mov bx,0
	
	mov cx,6
circ:
	mov al,[bx+3]
	and al,11011111b
	mov [bx+3],al
	add bx,16
	loop circ
	
	mov ax,4c00h
	int 21h
code ends
end start
```
        3.编程，给定数据段data，将data段中的每个单词改为大写字母
          1.【loop指令cx-1之后，在判断是否为0】
          2.双重循环用汇编怎么实现？
            应该在每次开始内循环的时候，将外层循环的cx的值保存起来，
            在执行外层循环的loop指令前，在恢复外层循环的cx数值。
            **可以用寄存器来临时保存，也可以用栈空间（内存）保存【没有多余的寄存器】
                更好的方法是使用：栈
              1.使用寄存器实现
```
assume cs:code,ds:data
data segment
	db 4,4,6,4,7,4;单词的字母数
	db '          ';补齐
	db '1. file         '
	db '2. edit         '
	db '3. search       '
	db '4. view         '
	db '5. options      '
	db '6. help         '
data ends

code segment
start:
	mov ax,data
	mov ds,ax
	mov bx,16
	mov si,0
	mov di,0
	
	mov cx,6;外层循环6次
outer:;外层循环
	mov dx,cx;用寄存器将外层循环的次数保存，C语言中是用栈来保存的
	
	mov cx,0
	mov cl,[di];内循环的次数	
	inner:;内层循环
		mov al,[bx][si+3]
		and al,11011111b
		mov [bx][si+3],al
		inc si
		loop inner
	
	add bx,16
	mov si,0
	inc di
	mov cx,dx;恢复外层循环的次数
	loop outer
	
	mov ax,4c00h
	int 21h
code ends
end start
```
              2.使用栈实现【更好的方法】
```
assume cs:code,ds:data,ss:stack
data segment
	db 4,4,6,4,7,4;单词的字母数
	db '          ';补齐
	db '1. file         '
	db '2. edit         '
	db '3. search       '
	db '4. view         '
	db '5. options      '
	db '6. help         '
data ends

stack segment
	dw 1,2,3,4,5,6,7,8
stack ends

code segment
start:
	mov ax,data
	mov ds,ax
	mov ax,stack
	mov ss,ax
	mov sp,16
	mov bx,16
	mov si,0
	
	mov cx,6;外层循环6次
outer:;外层循环
	push cx;将外层循环的次数保存
	
	mov cx,0
	mov cl,[di];内循环的次数	
	inner:;内层循环
		mov al,[bx][si+3]
		and al,11011111b
		mov [bx][si+3],al
		inc si
		loop inner
	
	add bx,16
	mov si,0
	inc di
	pop cx;恢复外层循环的次数
	loop outer
	
	mov ax,4c00h
	int 21h
code ends
end start
```
# 8、 数据处理的两个基本问题

[笔记](https://www.cnblogs.com/daker-code/p/12449094.html)	[数据处理的两个基本问题](https://blog.csdn.net/yyhshm520/article/details/142637965)	 

## ==bp==寄存器

[SS, SP, BP ](https://blog.csdn.net/bagboy_taobao_com/article/details/7370504)	

数据处理： **数据位置、数据长度**

- ==reg==:寄存器    ==sreg==：段寄存器
- ==bp相当于sp==，段地址默认在ss 

| ![](./汇编5-10.assets/Snipaste_2024-08-06_23-01-19.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-06_23-01-57.jpg) |
| ------------------------------------------------------- | ------------------------------------------------------- |
| ![](./汇编5-10.assets/Snipaste_2024-08-06_23-02-47.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-06_23-03-56.jpg) |
| ![](./汇编5-10.assets/Snipaste_2024-08-06_23-04-11.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-06_23-05-23.jpg) |
| ![](./汇编5-10.assets/Snipaste_2024-08-06_23-06-07.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-06_23-08-29.jpg) |

## 数据位置

- 数据在3个位置: cpu内部、内存、端口
  - 立即数
  - 寄存器
  - 段地址SA:偏移地址EA

- ==立即数==：数据在`cpu指令缓冲器`中

| ![](./汇编5-10.assets/Snipaste_2025-03-10_13-16-24.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-06_23-10-01.jpg)      |
| ------------------------------------------------------- | ------------------------------------------------------------ |
| ![](./汇编5-10.assets/Snipaste_2024-08-06_23-11-29.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-06_23-13-23.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2025-03-10_13-19-41.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-06_23-14-20.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2025-03-10_13-20-42.jpg) | ![强制给出段地址](./汇编5-10.assets/Snipaste_2025-03-10_13-23-12.jpg) |

## ==寻址方式总结==

[不同寻址方式演示](https://www.bilibili.com/video/BV1bL4y1K7sq/?p=38&vd_source=7346303e5e18677d7261c2c0c109ecfd) 

[汇编语言内存寻址方式](https://cloud.tencent.com/developer/article/2026604)   [操作数的寻址方式](https://cloud.tencent.com/developer/article/1592148)   [寄存器与七种寻址方式](https://cloud.tencent.com/developer/article/2046850) 

指令缓冲寄存器就是指令寄存器，因为图片里它里面放的是指令;  

**如何得到操作数**： ==取地址读取内容==

- ppt里有**直接寻址**过程演示 ：  `CS、IP取指，执行指令把ds:[000e]内容放入ax` 
  - 先读取指令放到指令缓冲器
  - 然后==执行指令,找内存单元地址，读取里面数据,保存到寄存器==
  - 指令`读一条执行一条`； 执行完才进行下一条指令读取
- 寄存器**间接寻址**找到的 ：指令执行时操作数在bx中，`2000f的内存单元000e，ax最后是000E`
- 寄存器**相对寻址**： ==ds + bx + 立即数== ; `[bx+1]` 
  - 1立即数，从==指令缓冲器==找
  - 加bx寄存器内容
  - 指令执行：得到操作数（==取地址读取内容==），合成后地址 `2000+000f+1 = 20010` ,ax最后 `8b00`

- **基址加变址**寻址： `[bx+si]`, 从==si寄存器==找； 合成后地址 `2000+000f+2 = 20011` ,ax最后 `078b`
- **相对基址加变址**寻址： `[bx+si+1]`, 从==si寄存器==找； 合成后地址 `2000+000f+2+1 = 20012` ,ax最后 `8b07`
  - ==ds + bx + si + 立即数== 
  - 例子里代码段和数据段在一起，当cs:ip指向内存单元时，是代码，当ds指向时，是数据

| ![](./汇编5-10.assets/Snipaste_2024-08-06_23-17-16.jpg)      | ![](./汇编5-10.assets/Snipaste_2024-08-06_23-17-45.jpg) |
| ------------------------------------------------------------ | ------------------------------------------------------- |
| ![CS、IP取指，执行指令把ds:[000e]内容放入ax](./汇编5-10.assets/Snipaste_2024-08-07_01-13-34.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-07_01-18-26.jpg) |
| ![](./汇编5-10.assets/Snipaste_2024-08-07_01-29-52.jpg)      | ![](./汇编5-10.assets/Snipaste_2024-08-07_01-39-15.jpg) |
| ![](./汇编5-10.assets/Snipaste_2024-08-07_01-41-15.jpg)      |                                                         |

## ==数据长度_ptr==

[汇编中的word ptr里的ptr是什么意思](https://jingyan.baidu.com/article/fb48e8bee68aba2f622e14a9.html) 	

- 8086是16位cpu，能处理字,为了兼容，也可处理字节数据
- 数据长度
  - 通过寄存器名 `ah、al`
  - 用操作符 `X ptr`：  `mov word ptr ds:[0],1`
    - ptr -- ==pointer== （指针）缩写  
    - `mov ax,word ptr [bx]`;  把内存单元的数据，赋予ax。 只是给出一个内存地址，不知道希望赋予ax的，是byte还是word，所以需要用word明确指出 
  - 其它方法
    - 有些==指令默认==访问字或字节单元


| ![](./汇编5-10.assets/Snipaste_2024-08-07_02-00-05.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-07_02-01-40.jpg) |
| ------------------------------------------------------- | ------------------------------------------------------- |
| ![](./汇编5-10.assets/Snipaste_2025-03-10_17-19-02.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-10_17-20-26.jpg) |
| ![](./汇编5-10.assets/Snipaste_2024-08-07_02-02-23.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-10_17-25-03.jpg) |
| ![](./汇编5-10.assets/Snipaste_2024-08-07_02-05-02.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-10_17-26-32.jpg) |
| ![](./汇编5-10.assets/Snipaste_2024-08-07_02-06-22.jpg) |                                                         |

## ==寻址方式的综合应用_必看==

[数据长度与寻址方式的综合应用](https://cloud.tencent.com/developer/article/2468815)	 [更灵活的定位内存地址的方法](https://blog.csdn.net/weixin_66578482/article/details/128560621)	 [王爽汇编语言markdown笔记](https://www.52pojie.cn/thread-1254113-1-1.html)	 

用`[bx+idata+si]`的方式来访问结构体中的数据

- 用`bx`定位整个结构体，用`idata`定位结构体中的某一个数据项，用 `si` 定位数组项中的每个元素
- 为此，汇编语言提供了更为贴切的书写方式。如：`[bx].idata`、`[bx].idata[si]`

C语言中

- `dec.cp[i]`，dec是一个变量名，指明`结构体变量的地址`，cp 是一个名称，指明`数据项cp的地址`，而i用来`定位cp中的每一个字符`
- `bx.10h[si]`对比一下，是不是很相似

| ![](./汇编5-10.assets/Snipaste_2025-03-11_15-58-30.jpg)      | ![](./汇编5-10.assets/Snipaste_2025-03-11_16-02-12.jpg)      |
| ------------------------------------------------------------ | ------------------------------------------------------------ |
| ![P是字符串的3次字节偏移](./汇编5-10.assets/Snipaste_2025-03-11_16-04-16.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-11_16-06-53.jpg)      |
| ![c语言中的数据](./汇编5-10.assets/Snipaste_2025-03-11_16-08-07.jpg) | ![汇编和c对比](./汇编5-10.assets/Snipaste_2025-03-11_16-10-21.jpg) |
| ![](./汇编5-10.assets/Snipaste_2025-03-11_16-12-40.jpg)      | ![](./汇编5-10.assets/Snipaste_2024-08-07_10-18-48.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2025-03-11_16-14-15.jpg)      |                                                              |

用bx定位整个结构体

## div

- 用乘法模拟除法: `20/4=5  4*1直到乘5等于20`, 乘法用加法模拟
- ==被除数==：默认放在`ax(16b)` 或 `dx和ax(32b)`
- ==除数==： 8或16位
- ==商==： 8位放在 `al`， 16位放在 `ax`
- ==余数==：8位放在 `ah`， 16位放在 `dx`

OPRD是"`Operand`"的缩写，表示操作数。操作数是指在`执行指令时`需要进行操作的数据    [汇编程序中OPRD什么意思](https://worktile.com/kb/ask/2645873.html) 	

- 操作数可以是寄存器、内存地址或立即数
- 使用`OPRD`来表示操作数的意义: 为了方便程序员编写和理解指令。通过给操作数进行命名，可以清晰地表达指令要进行的操作和所涉及的数据

无符号数除法指令 DIV(DIVision) 

- 格式: `DIV OPRD` 
- 功能: 实现两个无符号二进制数除法运算

| ![要看除数](./汇编5-10.assets/Snipaste_2024-08-07_10-22-44.jpg) | ![位数看除数](./汇编5-10.assets/Snipaste_2025-03-12_12-24-37.jpg) |
| ------------------------------------------------------------ | ------------------------------------------------------------ |
| ![](./汇编5-10.assets/Snipaste_2024-08-07_10-25-27.jpg)      | ![](./汇编5-10.assets/Snipaste_2025-03-12_12-33-03.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2025-03-12_12-33-56.jpg)      | ![](./汇编5-10.assets/Snipaste_2024-08-07_10-28-53.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2024-08-07_10-30-49.jpg)      | ![](./汇编5-10.assets/Snipaste_2024-08-07_10-31-36.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2024-08-07_10-32-08.jpg)      |                                                              |

## 伪指令dd

- `dd 双字` 32位
| ![](./汇编5-10.assets/Snipaste_2024-08-07_10-33-28.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-07_10-34-31.jpg) |
| ------------------------------------------------------- | ------------------------------------------------------- |
| ![](./汇编5-10.assets/Snipaste_2025-03-12_13-12-36.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-12_13-13-16.jpg) |
| ![](./汇编5-10.assets/Snipaste_2025-03-12_13-14-44.jpg) |                                                         |

## dup
- dup 方便定义数据; 定义栈段 
- dup 使用格式： `db 重复次数 dup (要重复的字节型数据)`    例：`db 200 dup (0)`
| ![](./汇编5-10.assets/Snipaste_2024-08-07_10-37-02.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-07_10-38-53.jpg) |
| ------------------------------------------------------- | ------------------------------------------------------- |
| ![](./汇编5-10.assets/Snipaste_2024-08-07_10-39-25.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-07_10-40-13.jpg) |
| ![](./汇编5-10.assets/Snipaste_2025-03-12_13-18-37.jpg) |                                                         |

## 寻址方式在==结构化数据访问==中的应用

- 数据覆盖写入到 `year summ ne ?? `  (表格)

| ![](./汇编5-10.assets/Snipaste_2025-03-12_13-21-13.jpg)      | ![](./汇编5-10.assets/Snipaste_2025-03-12_13-22-38.jpg) |
| ------------------------------------------------------------ | ------------------------------------------------------- |
| ![](./汇编5-10.assets/Snipaste_2025-03-12_14-12-24.jpg)      | ![](./汇编5-10.assets/Snipaste_2025-03-12_14-13-56.jpg) |
| ![目标存到es段](./汇编5-10.assets/Snipaste_2025-03-12_14-16-07.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-12_14-17-20.jpg) |
| ![](./汇编5-10.assets/Snipaste_2025-03-12_14-19-42.jpg)      | ![](./汇编5-10.assets/Snipaste_2025-03-12_15-09-33.jpg) |
| ![](./汇编5-10.assets/Snipaste_2025-03-12_15-10-43.jpg)      | ![](./汇编5-10.assets/Snipaste_2025-03-12_15-11-10.jpg) |
| ![](./汇编5-10.assets/Snipaste_2025-03-12_15-11-45.jpg)      |                                                         |



    本章对前面的所有内容是具有总结性的
    计算机是进行数据处理、运算的机器，那么有两个基本的问题就包含在其中：
        1.处理的数据在什么地方？
        2.要处理的数据有多长？
      这两个问题，在机器指令中必须给以明确或隐含的说明，否则计算机就无法工作
    8.1 bx、si、di、bp
        1.在8086CPU中，只有这4个寄存器（bx、bp、si、di）可以用在“[...]”
            中，用来进行内存单元的寻址
        2.在“[...]”中，这四个寄存器（bx、bp、si、di）可以单个出现，
            或者只能以以下4种组合出现
            1.bx和si
            2.bx和di
            3.bp和si
            4.bp和di
        3.错误的用法
            mov ax,[bx+bp]
            mov ax,[si+di]
        4.只要在[...]中使用寄存器bp，则指令中没有显性给出段地址，那么
            段地址就默认在ss中，比如：
            mov ax,[bp]       ax的值为栈空间中，偏移地址为bp的内存单元
            mov ax,[bp+常数]      
            mov ax,[bp+si]
            mov ax,[bp+si+常数]
    8.2 机器指令处理的数据所在的位置
        1.绝大部分机器指令进行数据处理的指令大致可分为3大类
            读取、写入、运算
        2.在机器指令这一层，并不关心数据的值是多少，而关心指令执行前一刻
            它将要处理的数据所在的位置
        3.指令在执行前，所要处理的数据可以在三个地方
            CPU内部（寄存器）、内存、端口
    8.3 汇编语言中数据位置的表达
        汇编语言中用三个概念来表达数据的位置
          1.立即数
          2.寄存器
          3.段地址（SA）和偏移地址（EA）
            1.存放段地址的寄存器可以是默认的，
                既可以是默认在ds中，也可以是在ss中（使用bp寄存器）
            2.存放段地址的寄存器也可以显性的给出
                mov ax,ds:[bp]
                mov ax,es:[bx]
                mov ax,ss:[bx+si]
                mov ax,cs:[bx+si+8]
    8.4 寻址方式
![avatar](./汇编5-10.assets/8.1.png)

    8.5 指令要处理的数据有多长？
        1.8086CPU的指令，可以处理两种尺寸的数据，byte和word
            所以在机器指令中要指明，指令进行的是字操作还是字节操作
        2.8086CPU确定数据长度的几种方法
            1.通过寄存器名指明要处理的数据的尺寸
                mov al,1        ;指明数据是字节型的
                mov bx,ds:[0]   ;指明数据是字型的
            2.在没有寄存器名存在的情况下，用操作符X ptr指明内存单元的长度
                X在汇编指令中可以为word或byte
                1.下面的指令中，用byte ptr指明了指令访问的内存单元是字节型单元
                  mov byte ptr ds:[0],1
                  inc byte ptr [bx]
                  inc byte ptr ds:[0]
                  add byte ptr [bx],2
                2.下面的指令中，用word ptr指明了指令访问的内存单元是字型单元
                  mov word ptr ds:[0],1
                  inc word ptr [bx]
                  inc word ptr ds:[0]
                  add word ptr [bx],2
            3.其他方法
                有些指令默认了访问的内存单元类型
                pop、push指令，一定是字型数据
        3.在没有寄存器参与的内存单元访问指令中，用word ptr或者byte ptr
            显性地指明所要访问的内存单元的长度，是非常有必须要的
            否则，CPU无法得知所要访问的单元是字单元，还是字节单元
    8.6 寻址方式的综合应用
    8.7 div指令
        1.div是除法指令（division），使用div作除法的时候，要求
            1.除数：8位或16位，在寄存器或内存单元中
            2.被除数：（默认）放在AX或DX和AX中
            3.除数与被除数的相互关系
                除数  被除数
                8位   16位（AX）
                16位  32位（DX+AX）
            4.结果存放的位置
                运算  8位  16位
                商    AL   AX
                余数  AH   DX
        2.div指令格式
            1.div 寄存器
            2.div 内存单元
            除数是寄存器或内存单元的内容
        3.div指令示例
            1.div byte ptr ds:[0]   ;被除数是16位，除数是ds:[0]的内容(8位)
              含义：(al)=(ax)/((ds)*16+0)的商
                   (ah)=(ax)/((ds)*16+0)的余数
           2.div word ptr es:[0]    ;被除数是32位，除数是es:[0]的内容(16位)
              含义：(ax)=[(dx)*10000H+(ax)]/((es)*16+0)的商
                   (dx)=[(dx)*10000H+(ax)]/((es)*16+0)的余数   
        4.利用除法指令计算100001/100
            1.被除数100001大于65535，要使用dx和ax两个寄存器联合存放
              即说要进行的16位的除法
            2.除数100小于255，可以在一个8位寄存器中存放，但是，因为被除数是32位
              除数应为16位，所以要用16位寄存器来存放除法100
            3.现将100001表示成十六进制数：186A1H，即dx中存放1H，ax中存放86A1H
```
mov dx,1
mov ax,86A1H
mov bx,100
div bx  ;默认除数是16位的
```
    8.8 伪指令dd
        1.dd是用来定义双字型数据的
        2.示例
            data segment
                db 1    ;字节型数据
                dw 1    ;字型数据
                dd 1    ;双字型数据
            data ends
        3.已知data段数据，用div计算data中第一个数据除以第二个数据后的结果，
            商存放在第3个数据的内存单元中
```
assume cs:code,ds:data
data segment
	dd 100001
	dw 100
	dw 0
data ends

code segment
start:
	mov ax,data
	mov ds,ax
	mov bx,0
	mov ax,[bx]     ;低位存放在ax中
	mov dx,[bx+2]   ;高位存放在dx中
	div word ptr [bx+4]
	mov [bx+6],ax   ;商存放在ax中，把ax中的内容放入内存中
	
	mov ax,4c00h
	int 21h
code ends
end start
```
    8.9 dup
        1.dup是一个操作符，在汇编语言中，同db、dw、dd等一样，也是有编译器识别处理的符号
        2.dup和db、dw、dd等数据定义伪指令配合使用的，用来进行数据的重复
        3.dup示例
            1.db 3 dup(0)   ;定义了3个字节，他们的值都是0
            2.db 3 dup(0,1,2)   ;定义了9个字节，他们是0、1、2、0、1、2、0、1、2
            3.db 3 dup('abc','ABC') ;定义了18个字节，相当于db'abcABCabcABCabcABC'
        4.dup的使用格式
            db 重复的次数 dup(重复的字节型数据)
            dw 重复的次数 dup(重复的字型数据)
            dd 重复的次数 dup(重复的双字型数据)
    【实验七】
    没调试成功
```
assume cs:code,ds:data,ss:stack,es:table

stack segment
	;空栈时，sp指向16
	dw 8 dup(0)
stack ends

data segment
	;表示21年的21个字符串
	;起始地址0，终止地址21*4-1：83
	db '1975','1976','1977','1978','1979','1980','1981','1982','1983'
	db '1984','1985','1986','1987','1988','1989','1990','1991','1992'
	db '1993','1994','1995'

	;表示21年公司总收入的21个双字型数据
	;起始地址21*4：84，终止地址21*4+21*4-1：167
	dd 16,22,382,1356,2390,8000,16000,24486,50065,97479,140417,197514
	dd 345980,590827,803530,1183000,1843000,2759000,3753000,4649000,5937000

	;表示21年公司雇员人数的21个字型数据
	;起止地址21*8：168，终止地址21*8+21*2-1：209
	dw 3,7,9,13,28,38,130,220,476,778,1001,1442,2258,2793,4037,5635,8226
	dw 11542,14430,15257,17800
data ends

table segment
	db 21 dup('year summ ne ?? ')
table ends



code segment
start:
	mov ax,data
	mov ds,ax
	
	mov ax,table
	mov es,ax
	
	mov ax,stack
	mov ss,ax
	mov sp,16
	
	mov si,0
	mov di,0
	mov bx,0
	mov bp,0 
	
	mov cx,21
outer:

	push si
	add si,si
	mov ax,ds:[bp]
	mov es:[bx][di],ax
	mov ax,ds:84[bp]
	mov es:[bx][di+5],ax
	pop si
	mov al,168[si]
	mov es:[bx][di+10],al
	inc si
	add di,2
	push si
	add si,si
	mov ax,ds:[bp]
	mov es:[bx][di],ax
	mov ax,ds:84[bp]
	mov es:[bx][di+5],ax
	pop si
	mov al,168[si]
	mov es:[bx][di+10],al
	inc si
	add di,2
	
	add bx,16
	loop outer
	
	mov ax,4c00h
	int 21h
code ends
end start
```
# 9、 转移指令的原理

[原码、反码、补码的深入理解](https://zhuanlan.zhihu.com/p/694564846)   [补码](https://baike.baidu.com/item/%E8%A1%A5%E7%A0%81/6854613?fr=ge_ala)   [计算机组成原理之源码、反码、补码详解及Byte结构](https://www.jb51.net/diannaojichu/883430.html)   [原码 补码_百度搜索](https://www.baidu.com/s?ie=UTF-8&wd=%E5%8E%9F%E7%A0%81%20%E8%A1%A5%E7%A0%81) 

[汇编jmp指令的理解与用法](https://blog.csdn.net/m0_63943951/article/details/124637842)	

> 1. 根据位移进行转移： `short near`
> 2. 转移的目的地址在指令中 :  `far`
> 3. 转移的地址在寄存器中 : `jump bx`  寄存器里放的是 `IP` 的值
> 4. 转移的地址在内存中:  `jmp word ptr 内存单元地址`,  `jmp dword ptr 内存单元地址`



- 转移指令: 高级语言里的 `goto`
- 过程： 相当于高级语言调用`函数` 
- ==offset== : `取得标号的偏移地址`；  例子：段地址从0开始，mov指令3个字节
- ==jmp==： 无条件跳转
  - 短内短转移： `ip` 修改范围，1个字节，2^8=256；  ==8位位移==   给的是位移，偏移地址  `jmp short 标号`
  - 段内近转移：==16位位移==  给的是位移，偏移地址  `jmp near ptr 标号`
  - 段间转移： 给的是目标地址，段与段跳转   `jmp far ptr 标号`

## offset

- `offset` : **获取标号或变量的偏移地址**;   相对于`代码段起始地址`的偏移量 
  - 格式: `OFFSET Var/label`
  - 功能: 回送变量或标号的偏移地址值


| ![](./汇编5-10.assets/Snipaste_2024-08-07_11-07-34.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-07_11-08-55.jpg) |
| ------------------------------------------------------- | ------------------------------------------------------- |
| ![](./汇编5-10.assets/Snipaste_2024-08-07_11-09-45.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-12_23-40-06.jpg) |
| ![](./汇编5-10.assets/Snipaste_2025-03-12_23-42-25.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-12_23-43-29.jpg) |

## jmp

### 原码、反码、补码

[好：原码、反码、补码的转换规则以及计算机内有符号数的计算方式](https://www.bilibili.com/video/BV1aB4y177Ef/?vd_source=7346303e5e18677d7261c2c0c109ecfd)	  [补码反码原码-bilibili](https://search.bilibili.com/all?vt=24726058&keyword=%E8%A1%A5%E7%A0%81%E5%8F%8D%E7%A0%81%E5%8E%9F%E7%A0%81&search_source=5)	[原码、反码、补码](https://www.bilibili.com/video/BV1Y7411t7Fj/?spm_id_from=333.788.recommend_more_video.0) 	

[原码反码补码在线工具](https://www.cnblogs.com/tsuish/p/16276562.html)  [补码 反码计算器](https://www.jisuanla.com/digital-computation/1s-2s-complement-calculator.php)	[二进制补码计算器_补码运算器](https://www.json.cn/gongju-numbers/twos_complement.html)	 [原码/反码/补码在线计算器](https://cloud.tencent.com/developer/article/2064484) 		

为了==解决计算机计算减法==的问题

- 计算机运算器中只有加法器，将减法转换为加法来计算
- 原码：数的二进制形式,首位为标志位,正数标志位为 `0`，负数 `1` ,数字转为二进制后补够`8`位 
- 反码：正数原、反、补码相同，为原码本身,  not取反
  - `负数标志位不变，其它取反`
- 补码：在反码基础上加一

| ![](./汇编5-10.assets/Snipaste_2025-03-14_12-04-41.jpg)      | ![](./汇编5-10.assets/Snipaste_2025-03-14_12-07-14.jpg)      |
| ------------------------------------------------------------ | ------------------------------------------------------------ |
| ![](./汇编5-10.assets/Snipaste_2025-03-14_12-09-23.jpg)      | ![](./汇编5-10.assets/Snipaste_2025-03-14_12-11-57.jpg)      |
| ![加法](./汇编5-10.assets/Snipaste_2025-03-14_14-34-11.jpg)  | ![负数时2个数补码相加，再转换为原码](./汇编5-10.assets/Snipaste_2025-03-14_14-35-30.jpg) |
| ![变成2个正数相加](./汇编5-10.assets/Snipaste_2025-03-14_14-37-49.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-14_14-38-54.jpg)      |

**跳转距离**

- ==段间==转移: `远转移`
  - 转移的==目的地址在指令中==的jmp指令, 因为debug中能看到 `jmp obbd:010b` 
  - 给的是==目标地址==: `段：偏移地址`
  - **同时修改cs和ip**    `jmp far ptr 标号`
  
- 段内==短转移== : `2^8` 一个字节, `-128~127`,   8位位移
  - 8086有20位地址总线，但cpu只有16位
  - 机器码给的是==偏移地址==,        `（IP）=（IP）＋8位位移量` , 等号右边的ip是 jump指令的下一条指令地址
    - 即 ”跳转目的地的指令的偏移地址(0008) = `jmp下一条指令的偏移地址`(0005) + jmp机器码包含的”03“偏移量“。
  - `JMP SHORT 转移地址标号` 
  - **只修改ip的值**，ip的修改范围为-128~127(负数跳转方向是jmp指令之前，否则是之后)，**8位的位移** 
  - 标号处`inc ax`偏移地址是0008，jmp就是0008； 机器码中确没有0008
  - `补码`是为了方便负数计算
  
- 段内==近转移==    16位位移     `（IP）=（IP）＋16位位移量`
  - `jmp near ptr 标号` ;**只修改ip的值**，ip的修改范围为-32768~32767，**16位的位移**


### 短内短转移

[jmp怎么改变ip](https://blog.csdn.net/m0_63943951/article/details/124637842) 	[好：jmp指令](https://blog.csdn.net/m0_50525151/article/details/129646740)	

==**短转移的原理**==：改变IP； 即：`（IP）=（IP）＋8位位移量` , 等号右边的ip是 jump指令的下一条指令地址

- ==相对于当前IP的转移位移（偏移）== 
- 式子右边（IP）为JMP后第一个字节的偏移地址；==8位位移量==为JMP机器指令中EB后接的16进制数值
- EB后面的`03`是jmp将要跳转到的那条指令的地址(076A:0008)， 相对于==jmp指令==的==下一条指令地址==(076A:0005)==的偏移地址==(位移的距离)  
  - 即 ”跳转目的地的指令的偏移地址(0008) = `jmp下一条指令的偏移地址`(0005) + jmp机器码包含的”03“偏移量“。

| ![](./汇编5-10.assets/Snipaste_2024-08-07_11-20-11.jpg)      | ![](./汇编5-10.assets/Snipaste_2024-08-07_11-21-08.jpg)      |
| ------------------------------------------------------------ | ------------------------------------------------------------ |
| ![](./汇编5-10.assets/Snipaste_2025-03-13_13-01-04.jpg)      | ![inc ax偏移地址是0008，jmp就是0008](./汇编5-10.assets/Snipaste_2024-08-07_11-26-43.jpg) |
| ![](./汇编5-10.assets/Snipaste_2025-03-13_13-04-28.jpg)      | ![](./汇编5-10.assets/Snipaste_2025-03-13_13-06-21.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2025-03-13_13-06-49.jpg)      | ![3+2共5个字节后到地址000A](./汇编5-10.assets/Snipaste_2025-03-13_13-08-38.jpg) |
| ![](./汇编5-10.assets/Snipaste_2024-08-07_11-29-15.jpg)      | ![](./汇编5-10.assets/Snipaste_2025-03-13_13-11-56.jpg)      |
| ![标号处的地址B-8=3](./汇编5-10.assets/Snipaste_2025-03-13_13-20-06.jpg) |                                                              |



```assembly
assume cs:cod
cod segment
  start:mov ax,0
	jmp short s
	add ax,1
  s:inc ax
cod ends
end start
```

### 短内近转移

- 转移地址在 ==16位寄存器== `jmp 16位寄存器`

| ![](./汇编5-10.assets/Snipaste_2025-03-14_14-42-55.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-07_11-44-25.jpg) |
| ------------------------------------------------------- | ------------------------------------------------------- |

### 段间转移-远转移

- 转移的==目的地址在指令中==的jmp指令, 因为debug中能看到 `jmp obbd:010b` 
  - 同时修改`cs和ip`, 要加段地址


| ![](./汇编5-10.assets/Snipaste_2024-08-07_14-58-36.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-07_14-59-40.jpg)      |
| ------------------------------------------------------- | ------------------------------------------------------------ |
| ![](./汇编5-10.assets/Snipaste_2025-03-15_00-13-53.jpg) | ![用的段：偏移](./汇编5-10.assets/Snipaste_2025-03-15_00-15-06.jpg) |
| ![](./汇编5-10.assets/Snipaste_2024-08-07_15-02-51.jpg) |                                                              |

### 转移地址在寄存器的jump指令

- 寄存器里放的是 `IP` 的值, `jump bx` 
- 同时修改cs、ip： `jmp 2ae3:3`
- 仅修改ip： `jmp ax`

![](./汇编5-10.assets/Snipaste_2024-08-07_15-05-01.jpg)

### 转移地址在内存的jump指令

两种格式

- ==段内转移== `jmp word ptr 内存单元地址`
  - 内存单元给的是`转移的目的`偏移地址
- ==段间转移== `jmp dword ptr 内存单元地址`
  - 双字，内存高地址是转移目的的段地址，低地址是偏移地址


| ![](./汇编5-10.assets/Snipaste_2024-08-07_15-11-35.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-15_00-29-46.jpg) |
| ------------------------------------------------------- | ------------------------------------------------------- |
| ![](./汇编5-10.assets/Snipaste_2024-08-07_15-13-54.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-15_00-36-35.jpg) |

## ==条件跳转==jcxz

条件跳转

- ==jcxz==, 当(cx)`=0,就跳转`到标号处执行， 不为0就不跳, `jump cx zero`
- ==loop==, (cx)=0,不跳转，和 `jcxz`相反
- 条件转移指令都是短转移, `2^8` 一个字节, `-128~127`,   8位位移

| ![](./汇编5-10.assets/Snipaste_2024-08-07_15-17-11.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-07_15-18-41.jpg) |
| ------------------------------------------------------- | ------------------------------------------------------- |
| ![](./汇编5-10.assets/Snipaste_2025-03-17_14-10-09.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-17_14-12-35.jpg) |
| ![](./汇编5-10.assets/Snipaste_2024-08-07_15-23-35.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-07_15-24-05.jpg) |
| ![](./汇编5-10.assets/Snipaste_2025-03-17_14-16-52.jpg) |                                                         |

### 根据位移进行转移的意义

- 机器码不包含目标地址，而是包含偏移地址
- 这样设计，方便程序段在内存中浮动装配

| ![](./汇编5-10.assets/Snipaste_2024-08-07_15-25-54.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-07_15-27-24.jpg) |
| ------------------------------------------------------- | ------------------------------------------------------- |
| ![](./汇编5-10.assets/Snipaste_2025-03-17_14-25-17.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-17_14-25-47.jpg) |
| ![](./汇编5-10.assets/Snipaste_2025-03-17_14-26-16.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-17_14-26-56.jpg) |

## 实验八、九

[内存地址和显存地址](https://www.zhihu.com/question/36397999/answer/67615103)	 [计算机底层是如何访问显卡的](https://www.cnblogs.com/mahuan2/p/4767642.html)   [显存与内存地址空间冲突及解决方法](https://blog.csdn.net/pizi0475/article/details/45056583)	

[实验9 ](https://blog.csdn.net/ntr851217/article/details/130785133)  [实验九](https://blog.csdn.net/weixin_51187533/article/details/123324929)	[实验九](https://zhuanlan.zhihu.com/p/696136964)	 [实验九](https://blog.51cto.com/u_15878568/5859634)		 	

八:  把s2处的指令给ax，然后ax再覆盖s处，`nop`被 `jmp short s1`覆盖，根据短转移，向上跳转10字节空间，就从 `mov ax,4c00h` 开始执行, 而不是跳到 `s1`标号处执行

- 把s2处的指令 `f6eb`覆盖到s处，f6是位移，是负数的补码

九: 向显存(`B8000H ~ BFFFFH`)写入数据，会显示出来

- $32kB, 80*25$ ,25行，每行80字符,每个字符256个属性
- 每个字符占2字节，存ascii和属性, 所以每页4000字节, $2*80*25=4000$ 

| ![](./汇编5-10.assets/Snipaste_2025-03-17_14-42-23.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-17_16-37-19.jpg) |
| ------------------------------------------------------- | ------------------------------------------------------- |
| ![](./汇编5-10.assets/Snipaste_2025-03-17_17-06-07.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-17_17-06-52.jpg) |
| ![](./汇编5-10.assets/Snipaste_2025-03-17_17-07-44.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-17_17-08-43.jpg) |

实验8

```assembly
assume cs:codesg
codesg segment
		mov ax,4c00H
		int 21h

start:  mov ax,0
	s:  nop              ;jmp short s1
		nop
		mov di,offset s
		mov si,offset s2
		mov ax,cs:[si]	 ; ax,s2
		mov cs:[di],ax	 ; s, ax,    用jmp short s1覆盖s处的nop
		
	s0:	jmp short s
	
	s1:	mov ax,0
		int 21h
		mov ax,0
		
	s2: jmp short s1
		nop
		
codesg	ends
end start
```

实验9

段地址： `b8000+720=b872`

外循环行，内循环（字符+属性）,循环前暂存寄存器，初始化数据

```assembly
assume cs:code,ds:data,ss:stack

data segment
	db 'welcome to masm!'	;共16byte, 把字符和属性放入显存缓冲区
	db 02h,24h,71h  		; 三个颜色属性
data ends

stack segment
	db 16 dup(0)			; dw 8 dup(0), 用栈段保存状态
stack ends

code segment

start:	mov ax,data
		mov ds,ax
	
		mov ax,stack
		mov ss,ax
		mov sp,10h  		;指向栈顶
		
		xor bx,bx			;bx清零，用来索引颜色
		mov ax,0b872h		;屏幕12行中间的段起始位置
		
		
		mov cx,10    		; 外循环显示3行
	s3: push cx				;保存外循环相关寄存器值,防止在内循环被破坏
		push ax
		push bx				;bx索引颜色
		
		
		mov es,ax			;es为12行中间显存段起始位置
		
		mov si,0			;si索引代码列字符(偏移)
		mov di,0  			;si索引目标列字符(偏移)
		
		
		mov cx,10h			;16个字符要循环16次				
	s1:	mov al,ds:[si] 		;将date段中的字符一个一个传入es显存中
		mov es:[di],al 		
		
		inc si
		add di,2  			;显存1个字符2个字节
		loop s1				;内循环存放字符
		
		
		;为放属性做准备：初始化
		mov di,1
		pop bx
		mov al,ds:10h[bx]
		inc bx				;指向下一个颜色
		
		mov cx,10h					
	s2:	mov es:[di],al 	
		add di,2  			
		loop s2				;内循环存放属性
		
		
		pop ax  			;每行段地址pop出来
		add ax,0ah			;加上a指向第二行,段地址加0ah，相当于偏移地址加a0h,160个字节
		pop cx
		loop s3
		
		mov ax,4c00h
		int 21h
code ends

end start
```




    8086CPU的转移指令分为以下几类：
        1.无条件跳转指令（如：jmp）
        2.条件跳转指令
        3.循环指令（如：loop）
        4.过程，就像C语言中的函数
        5.中断
    9.1 操作符offset
        操作符offset在汇编语言中由编译器处理，它的功能是取标号的偏移地址
        如：s:mov ax,offset s
    9.2 jmp指令
        1.无条件转移，可以只修改ip，也可以同时修改cs和ip
            1.【jmp 段地址:偏移地址】  可以用来同时修改CS和IP
                指令中的段地址修改CS
                偏移地址修改IP
                这种用法编译器不认识，只能做在debug中使用
            2.【jmp 某一合法的寄存器】   仅修改IP的内容
                比如：jmp ax 或者 jmp bx（类似于mov IP ax）
        2.jmp指令要给出两种信息：
            1.转移的目的地址
            2.转移的距离（段间转移、段内短转移、段内近转移）
    9.3 依据位移进行转移的jmp指令
        1.jmp short 标号【转到标号处执行指令,段内短转移】
          此格式实现的是：段内短转移，它对ip的修改范围为-128~127
        2.也就是说，它向前转移时可以最多越过128个字节，负数使用补码表示
          向后转移可以最多越过127个字节
        3.CPU不需要目的地址就可以实现对ip的修改
            jmp指令的机器码中不包含目的地址，但是可以实现跳转
            实现的方式，是在原地址的基础上进行一个偏移量，即位移
        4.还有一种和指令“jmp short 标号”功能类似的指令格式：
            jmp near ptr 标号，它实现的是段内近转移 
            功能为：(ip)=(ip)+16位位移
            jmp short 标号是8位的位移，而jmp near ptr 标号是16位位移
    9.4 转移的目的地址在指令中的jmp指令
        前面讲的jmp指令，其对应的机器码中并没有转移的目的地址，而是相对于当前ip的转移位移
        1.指令“jmp far ptr 标号”
            实现的是段间转移，又称为远转移,这时机器码中应该明确给出【段地址】
        2.指令“jmp far ptr 标号”功能如下：
            (CS)=标号所在段的段地址
            (IP)=标号所在段中的偏移地址
            far ptr 指明了指令用标号的段地址和偏移地址修改cs和ip
    9.5 转移地址在寄存器中的jmp指令
        指令格式：jmp 16位寄存器
        功能：修改ip寄存器中的值，把16位寄存器中的值送入到ip寄存器中
    9.6 转移地址在内存中的jmp指令
        转移地址在内存中的jmp指令有两种格式：
        1.jmp word ptr 内存单元地址（段内转移）
            功能：将内存中的那个字视为一个偏移地址，然后跳转到那个偏移地址
            与【jmp 寄存器】功能相似
            内存单元地址可用寻址方式的任意格式给出
        2.jmp dword ptr 内存单元地址（段间转移）
            (ip)=(内存单元地址)   ;双字中的低位字是给ip的
            (cs)=(内存单元地址+2) ;双字中的高位字是给cs的
            跟【jmp 段地址:偏移地址】功能类似
            内存单元地址可用寻址方式的任意格式给出
            **补充：不能直接向内存单元中加入立即数
                要通过寄存器，把立即数加进去
    9.7 jcxz指令
        1.有条件跳转指令，所有的有条件跳转指令都是短转移
            对应的机器码中包含转移的位移，而不是目的地址。对ip的修改范围都为：-128~127
            **另一个有条件跳转指令【loop指令】
        2.指令格式：jcxz 标号
            如果（cx）=0，则跳转到标号处执行
        3.jcxz 标号 指令的操作：
            1.当(cx)=0时，(ip)=(ip)+8位位移
            2.当(cx)!=0时，什么也不做（程序继续向下执行）
    9.8 loop指令
        1.循环指令，所有的循环指令都是短转移，在对应的机器码中包含转移的位移
        2.指令格式：loop 标号
        3.指令的内部操作
            1.cx=cx-1
            2.如果cx!=0，(ip)=(ip)+8位位移，跳转
            3.(cx)=0，什么也不做，程序向下执行
            cx用来控制循环的次数
    9.9 根据位移进行转移的意义
        1.根据位移进行转移，这样设计，方便了程序段在内存中的浮动装配
            可以实现代码的复用
        2.如果在机器码中直接给出【段地址:偏移地址】，
            这段程序在内存中换一个位置，则会运行不正确
        3.段内近转移、段内短转移都是根据位移进行转移，一共有四种方式
            1.jmp short ptr 标号
            2.jmp near ptr 标号
            3.jcxz 标号
            4.loop 标号
    9.10 编译器对转移位移超界的检测
        注意，根据位移进行转移的指令，他们的转移范围会受到限制
        如果在源程序中出现了转移范围超界的问题，在编译的时候，编译器将报错
    【实验八、九】【这个实验要重点看】
# 10、 call和ret指令

[call指令和ret指令](https://cloud.tencent.com/developer/article/2145728) 

小结： `函数调用，通过栈暂存返回地址，` 执行完，ret返回到调用位置

> call和ret配合使用，使函数的调用和返回成为可能,它们都是转移指令

## ret、retf

- call时当前状态入栈，ret返回时从栈中取回状态,  `ret修改cs、ip`实现转移

- ret  ==近转移==, 用栈中数据修改`IP`内容   相当于`pop ip`
  - `IP` 出栈，然后 `（SP）+2`
  - 例子里，将栈顶值0给IP，跳转到`move ax,4c00h`
  - **Return** 返回
  
- retf ==远转移==                         `pop ip   pop cs`

| ![](./汇编5-10.assets/Snipaste_2024-08-08_09-49-29.jpg)      | ![](./汇编5-10.assets/Snipaste_2024-08-08_10-35-02.jpg)      |
| ------------------------------------------------------------ | ------------------------------------------------------------ |
| ![取回IP状态](./汇编5-10.assets/Snipaste_2024-08-08_10-36-30.jpg) | ![取回IP和cs状态](./汇编5-10.assets/Snipaste_2024-08-08_10-36-59.jpg) |
| ![](./汇编5-10.assets/Snipaste_2024-08-08_10-38-35.jpg)      | ![](./汇编5-10.assets/Snipaste_2025-03-17_18-23-56.jpg)      |
| ![将栈顶值0给IP，跳转到move ax,4c00h](./汇编5-10.assets/Snipaste_2025-03-17_18-30-22.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-17_18-33-13.jpg)      |
| ![段地址入栈](./汇编5-10.assets/Snipaste_2025-03-17_18-33-41.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-17_18-35-52.jpg)      |

## call

- **call指令2步操作**： 先`push`入栈，保存cs和ip；  再`jmp`跳转去执行后面的指令
- `1、(sp)=(sp)-2,  ((ss)*16+(sp))=(ip) ip入栈  2、(ip)=(ip)+16位位移 再跳转`

call指令是==调用函数==的意思，让处理器从新的内存地址开始执行，过程==使用RET来返回，让处理器回到调用点==上。

call会将==返回地址压入栈==，然后再将要==被调用的地址复制到IP==寄存器里。当==RET指令被执行的时候==，就会==从栈中弹出返回地址到IP==寄存器内

- `call 标号`：    		 push ip， `jmp near ptr 标号`
- `call far ptr 标号`：     段间转移,  push cs， push ip， `jmp far ptr 标号`
- `call 16位寄存器`:        push ip, `jmp 16位寄存器`
- 转移地址在内存中的call:   `call word ptr 内存单元地址`,  push ip, `jmp word ptr 内存单元地址`

### 依据位移进行转移的call指令
| ![](./汇编5-10.assets/Snipaste_2024-08-08_10-41-36.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-08_10-44-05.jpg)      |
| ------------------------------------------------------- | ------------------------------------------------------------ |
| ![](./汇编5-10.assets/Snipaste_2024-08-08_10-46-05.jpg) | ![csip入栈，然后跳转取指令](./汇编5-10.assets/Snipaste_2024-08-08_11-11-51.jpg) |
| ![](./汇编5-10.assets/Snipaste_2025-03-18_13-58-43.jpg) |                                                              |

### 转移的目的地址在指令中的call指令(远转移)

- CS、IP入栈，然后 `jmp far ptr 标号` 

| ![](./汇编5-10.assets/Snipaste_2024-08-08_11-16-22.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-08_11-17-10.jpg) |
| ------------------------------------------------------- | ------------------------------------------------------- |
| ![](./汇编5-10.assets/Snipaste_2025-03-18_14-12-18.jpg) |                                                         |

### 转移地址在寄存器的call指令

- `IP`入栈，然后 `jmp 16位寄存器`

![](./汇编5-10.assets/Snipaste_2024-08-08_11-18-17.jpg)

### 转移地址在内存的call指令

| ![](./汇编5-10.assets/Snipaste_2024-08-08_11-19-48.jpg)      | ![](./汇编5-10.assets/Snipaste_2025-03-18_16-10-11.jpg) |
| ------------------------------------------------------------ | ------------------------------------------------------- |
| ![](./汇编5-10.assets/Snipaste_2024-08-08_11-21-30.jpg)      | ![](./汇编5-10.assets/Snipaste_2024-08-08_11-31-51.jpg) |
| ![低16字节放ip,高16放cs](./汇编5-10.assets/Snipaste_2024-08-08_11-36-20.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-18_16-14-21.jpg) |

## call和ret配合使用

call执行时先push ip,再jmp，000e到标号s有5个字节位移

| ![](./汇编5-10.assets/Snipaste_2025-03-18_16-15-00.jpg) | ![1+1循环3次等于8](./汇编5-10.assets/Snipaste_2025-03-18_16-17-23.jpg) |
| ------------------------------------------------------- | ------------------------------------------------------------ |
| ![](./汇编5-10.assets/Snipaste_2025-03-18_16-17-42.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-18_16-18-33.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2025-03-18_16-19-25.jpg) | ![call执行时先push ip,再jmp，000e到标号s有5个字节位移](./汇编5-10.assets/Snipaste_2025-03-18_16-25-43.jpg) |
| ![](./汇编5-10.assets/Snipaste_2025-03-18_16-27-19.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-18_16-28-24.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2025-03-18_16-28-55.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-18_16-30-07.jpg)      |
| ![](./汇编5-10.assets/Snipaste_2025-03-18_16-32-15.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-08_11-43-55.jpg)      |

## mul乘法指令

`multiply  multiplication`

- 相乘的2个数： 要么都是8位，要么都是16位; 8位乘数默认在al中,16位ax中；  被乘数在寄存器或内存中
  - 100*10 : `mov al,100  mov bl,10  mul bl`
- 结果：8位放ax中，16位：高位放DX，低位放AX

| ![](./汇编5-10.assets/Snipaste_2024-08-08_11-48-12.jpg)      | ![](./汇编5-10.assets/Snipaste_2024-08-08_11-48-49.jpg) |
| ------------------------------------------------------------ | ------------------------------------------------------- |
| ![](./汇编5-10.assets/Snipaste_2025-03-18_16-39-00.jpg)      | ![](./汇编5-10.assets/Snipaste_2025-03-18_16-42-08.jpg) |
| ![结果高16位放dx，低16位放在ax中](./汇编5-10.assets/Snipaste_2025-03-18_16-42-55.jpg) |                                                         |

## 模块化程序设计

- 参数和结果传递： 存到寄存器，效率高
- 多个参数或返回值： 放到内存，空间大
- 使用栈传递参数

| ![](./汇编5-10.assets/Snipaste_2025-03-18_17-16-11.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-18_17-16-45.jpg) |
| ------------------------------------------------------- | ------------------------------------------------------- |

### 参数和结果传递的问题

参数和结果存在寄存器

批量数据传递存到内存

| ![](./汇编5-10.assets/Snipaste_2024-08-08_11-54-03.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-18_17-18-47.jpg) |
| ------------------------------------------------------- | ------------------------------------------------------- |
| ![](./汇编5-10.assets/Snipaste_2025-03-18_17-20-19.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-18_17-21-21.jpg) |
| ![](./汇编5-10.assets/Snipaste_2024-08-08_11-56-54.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-18_17-25-03.jpg) |

### 批量数据的传递

| ![](./汇编5-10.assets/Snipaste_2024-08-08_11-58-20.jpg) | ![](./汇编5-10.assets/Snipaste_2024-08-08_11-59-43.jpg) |
| ------------------------------------------------------- | ------------------------------------------------------- |
| ![](./汇编5-10.assets/Snipaste_2025-03-18_17-28-28.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-18_17-29-12.jpg) |
| ![](./汇编5-10.assets/Snipaste_2024-08-08_12-00-35.jpg) |                                                         |

### 寄存器冲突的问题

实验十视频没有看,前面的视频都细看过

| ![](./汇编5-10.assets/Snipaste_2025-03-18_17-31-23.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-18_17-32-11.jpg) |
| ------------------------------------------------------- | ------------------------------------------------------- |
| ![](./汇编5-10.assets/Snipaste_2025-03-18_17-32-49.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-18_17-34-03.jpg) |
| ![](./汇编5-10.assets/Snipaste_2025-03-18_17-35-18.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-18_17-35-42.jpg) |
| ![](./汇编5-10.assets/Snipaste_2025-03-18_17-36-36.jpg) | ![](./汇编5-10.assets/Snipaste_2025-03-18_17-37-43.jpg) |



    call和ret指令都是转移指令，它们都能修改ip，或同时修改cs和ip
    10.1 ret和ref
        1.ret指令用栈中的数据，修改ip的内容，从而实现【近转移】
            CPU执行ret指令时，进行下面两步操作：
            1.(ip)=((ss)*16+(sp))   ;ip的值修改为栈顶的内容
            2.(sp)=(sp)+2           ;栈顶移动
        2.retf指令用栈中的数据，修改cs和ip的内容，从而实现【远转移】
            CPU执行retf指令时，进行下面四步操作
            1.(ip)=((ss)*16+(sp))   ;ip的内容修改为栈顶的内容
            2.(sp)=(sp)+2           ;栈顶移动
            3.(cs)=((ss)*16+(sp))   ;cs的内容修改为栈顶移动之后，栈顶的内容
            4.(sp)=(sp)+2           ;栈顶移动
            栈顶的两个字，低位字修改为ip，高位字修改为cs
        3.可以看出，如果我们用汇编语法来解释ret和retf指令，则
            1.CPU执行ret指令，相当于
                pop ip
            2.执行retf指令时，相当于
                pop ip
                pop cs
    10.2 call指令
        1.call指令经常跟ret指令配合使用，因此CPU执行call指令，进行两步操作：
            1.将当前的ip或cs和ip压入栈中
            2.转移
        2.call指令不能实现短转移，除此之外，
            call指令实现转移的方法和jmp指令的原理相同
        【依据位移进行转移的call指令】
        3.CPU执行“call 标号”这种格式的call指令时，进行如下操作：
            1.(sp)=(sp)-2         ;栈顶移动
            2.((ss)*16+(sp))=(ip) ;当前ip内容压栈
            3.(ip)=(ip)+16位位移   ;跳转到标号处
        4.call指令格式：call 标号
            相当于执行：
            push ip
            jmp near ptr 标号
    10.4 转移的目的地址在指令中的call指令
        1.指令格式：call far ptr 标号
            实现的是段间转移
        2.执行这种格式的call指令时CPU的操作
            1.(sp)=(sp)-2           ;栈顶移动
            2.((ss)×16+(sp))=(cs)   ;先把cs压栈
            3.(sp)=(sp)-2           ;栈顶移动
            4.((ss)×16+(sp))=(ip)   ;然后把ss压栈
        3.CPU执行“call far ptr 标号”时，相当于进行
            push cs
            push ip
            jmp far ptr 标号
    10.5 转移地址在寄存器中的call指令
        1.指令格式：call 16位寄存器
        2.执行这种指令时，在CPU中的操作
            1.(sp)=(sp)-2
            2.((ss)×16+(sp))=(ip)
            3.(ip)=(16位寄存器)
        3.相当于
            push ip
            jmp 16位寄存器
    10.6 转移地址在内存中的call指令
        转移地址在内存中的call指令有两种格式：
        1.call word ptr 内存单元地址
            汇编语法解释
              push ip
              jmp word ptr 内存单元地址
        2.call dword ptr 内存单元地址
            汇编语法解释
              push cs   ;cs存放在高位
              push ip   ;ip存放在低位
              jmp dword ptr 内存单元地址
    10.7 call和ret的配合使用
    10.8 mul指令
        相乘的两个数；要么都是8位，要么都是16位
        1.8位：AL中和8位寄存器或内存字节单元中
            AL中的内容作为被乘数
            结果放在AX中
        2.16位：AX中和16位寄存器或内存字单元中
            AX中的内容作为被乘数
            结果放在DX（高位）和AX（低位）中。
        3.格式如下：
            mul 寄存器
            mul 内存单元（byte ptr或 word ptr指明是字还是字节）
    10.9 模块化程序设计
    10.10 参数和结果传递的问题
        【编程】计算data段中第一组数据的3次方，结果保存在后面一组dword单元中
```
data sgement
    dw 1,2,3,4,5,6,7,8
    dd 0,0,0,0,0,0,0,0 
data ends
```
```
10.11 批量数据的传递
    使用寄存器、内存、栈传递数据
    【编程】将一个全是字母，以0结尾的字符串，转化为大写
【实验十 编写子程序】
    1.显示字符串
    2.解决除法溢出问题
    3.数值显示
【课程设计1】
```
